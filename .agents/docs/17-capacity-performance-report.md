# 阶段 6 容量与性能验收报告

## 结论

- 在隔离项目 pnx-stage6-performance、生产同等资源上限、300 个虚构学生容量数据下，100 个独立登录会话完成 2,000 次典型读取请求，错误率 0%，总体 P95 为 341.754 ms，满足“非上传 API P95 < 500 ms、错误率 < 1%”。
- 20 路并发 multipart 分别通过 1 MiB 协议样本和 100 MiB 容量样本；100 MiB 场景共上传 2,000 MiB、140 个分片，错误率 0%。
- 2,130,706,432 字节近 2 GiB 单文件以 1 MiB 生成块、127 个分片完成，未把大文件写入仓库或整体载入内存。
- 实测后将 Backend 收敛为 4 个 Uvicorn Web Worker、每进程数据库连接池 8+4，并把生产 Backend CPU 上限从 2 核调整为 4 核。数据库迁移和业务 API 均未改变。

## 验收范围与隔离边界

- 日期：2026-08-25（Asia/Shanghai）。
- Compose 项目：pnx-stage6-performance；入口：http://127.0.0.1:58082。
- 数据卷、端口和虚构账号均与 pnx-training 隔离；未向局域网开发数据注入容量或故障数据。
- 密码只从权限 0600 的绝对路径文件读取；工具不输出账号、密码、Cookie、响应正文、上传 ID 或文件 ID。
- 对象内容由确定性可打印模式按 1 MiB 块流式生成；分片 SHA-256 使用 Base64，请求完成校验使用完整十六进制 SHA-256。

## 主机与运行配置

| 项目 | 实测配置 |
| --- | --- |
| 主机 | AMD Ryzen 7 7840HS，8 核 16 线程，30 GiB 内存 |
| 内核 / Docker | Linux 7.0.0-28-generic；Docker 29.6.0，API 1.55 |
| Backend | 4 个 Uvicorn Web Worker；4 CPU、1 GiB、256 PID |
| 数据库连接 | 每进程 pool_size=8、max_overflow=4；Web 理论上限 48，连同 Worker 仍低于 PostgreSQL 默认上限 |
| PostgreSQL | 2 CPU、2 GiB、256 PID |
| MinIO | 2 CPU、2 GiB、256 PID |
| Nginx | 0.5 CPU、256 MiB、128 PID |
| 分片 | 16,777,216 字节；每次最多预签名 10 片 |

## 确定性容量数据

| 数据 | 数量 |
| --- | ---: |
| 学生 / 管理员 | 300 / 10 |
| 通知 / 学生站内通知 | 100 / 30,000 |
| 作业 / 作业受众快照 | 40 / 12,000 |
| 赛事 / 赛题 / 历史队伍 | 10 / 10 / 100 |
| 提交聚合 / 不可变版本 | 12,090 / 24,180 |

容量种子只允许非生产环境，使用确定性 UUIDv7 和事务级幂等标记。作业与赛题扩展名已统一为无前导点的 txt、pdf、zip，和正式 API、上传策略一致。

## 100 会话读取结果

场景为 100 个独立学生 Session、5 轮，每轮读取工作台、未读通知、作业和赛事；分页大小 20，与真实学生页面一致。

| 指标 | 结果 |
| --- | ---: |
| 请求数 / 成功 / 错误 | 2,000 / 2,000 / 0 |
| 持续时间 | 3.532 s |
| 吞吐 | 566.286 req/s |
| P50 / P95 / P99 | 142.555 / 341.754 / 493.617 ms |
| 最大延迟 | 1,016.169 ms |

| 路径 | P50 | P95 | P99 | 错误率 |
| --- | ---: | ---: | ---: | ---: |
| 工作台 | 154.737 ms | 354.475 ms | 504.290 ms | 0% |
| 未读通知 | 117.696 ms | 336.579 ms | 493.678 ms | 0% |
| 作业 | 144.777 ms | 358.329 ms | 533.457 ms | 0% |
| 赛事 | 142.882 ms | 335.228 ms | 396.753 ms | 0% |

首轮曾因作业列表和赛事列表 N+1 查询、单 Web Worker 以及 2 CPU CFS 周期节流未达标。最终实现把作业提交摘要和赛事报名/队伍状态改为批量查询，并让工作台跳过未使用的分页 count；API 响应结构与业务规则未变。

## Multipart 结果

| 场景 | 结果 |
| --- | --- |
| 20 × 1 MiB | 20/20 成功，20 MiB；上传端到端 P95 414.175 ms；协议四阶段均零错误 |
| 20 × 100 MiB（最终限额） | 20/20 成功，2,000 MiB，140 片；10.671 s；187.421 MiB/s；端到端 P95 10,665.335 ms |
| 近 2 GiB 单文件（最终限额） | 2,130,706,432 字节，127 片；7.832 s；259.445 MiB/s；完成校验 2,192.246 ms；零错误 |

20×100 MiB 最终限额场景的请求阶段 P95：初始化 343.797 ms、预签名 232.394 ms、分片 PUT 2,959.566 ms、服务端完成校验 283.268 ms。分片 PUT 受 Nginx 0.5 CPU 硬上限节流，但无错误或超时，仍达到约 187 MiB/s 聚合吞吐。

## 资源与存储

以下峰值来自最终资源上限下的 20×100 MiB 场景；Docker CPU 100% 表示一个逻辑 CPU。

| 服务 | 峰值 CPU | 峰值内存 | 峰值 PID |
| --- | ---: | ---: | ---: |
| Backend | 68.60% | 531.40 MiB | 97 |
| PostgreSQL | 2.65% | 233.50 MiB | 41 |
| MinIO | 90.66% | 416.00 MiB | 38 |
| Nginx | 50.62% | 19.36 MiB | 17 |

- 最终容量环境 PostgreSQL 大小：41,916,083 字节；采样时连接数 34。
- 完成多轮协议与容量复测后，可用文件 102 个；MinIO 数据目录物理占用 12,672,666,855 字节。
- 生产 1 GiB Backend、2 GiB PostgreSQL/MinIO 和 256 MiB Nginx 内存上限均覆盖本次峰值；Backend CPU 上限必须保持 4 核，否则相同负载 P95 约 710 ms。

## 复现命令

读取工具：

    backend/.venv/bin/python infra/performance/read_load.py --base-url http://127.0.0.1:58082 --password-file <绝对路径密码文件> --sessions 100 --rounds 5

20×100 MiB：

    backend/.venv/bin/python infra/performance/multipart_load.py --base-url http://127.0.0.1:58082 --password-file <绝对路径密码文件> --uploads 20 --size-bytes 104857600

近 2 GiB：

    backend/.venv/bin/python infra/performance/multipart_load.py --base-url http://127.0.0.1:58082 --password-file <绝对路径密码文件> --uploads 1 --size-bytes 2130706432

## 限制与现场复验

- 本报告使用同宿主机回环 HTTP，验证应用、Nginx、PostgreSQL 和 MinIO 的服务端容量，不包含校园网络往返或生产 TLS 加密开销。
- 受信生产域名/证书、校园 SMTP、异机备份目标和独立告警接收方仍由部署方提供；材料到位后应在目标主机按相同命令重新建立现场基线。
- 近 2 GiB 验收为一个上限样本；20 并发能力由 100 MiB 场景独立验证，避免测试本身额外长期占用约 40 GiB。

