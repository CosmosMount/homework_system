# 账号活跃度、十天提醒与安全清理计划

## 状态

仓库实现与权威文档同步已完成，质量门、`0012` 迁移应用和运行态验收由当前总任务统一执行；本任务不自动删除任何现有账号。

## 背景与必要性

现有系统只在 `sessions.last_seen_at` 记录单个 Session 的最近请求时间，管理员“登录人员”页也只展示当前仍有效的 Session。Session 退出、撤销或过期后，管理员无法从账号维度判断用户最后一次进入系统的时间，也无法稳定筛出超过 10 天未进入的账号。

本次新增 AUTH-012，向管理员展示账号活跃度，并在管理概览提醒严格超过 10 天未进入系统的 `active` 账号；新增 AUTH-013，为这些账号提供受保护的永久删除入口。该能力不新增角色或账号状态，不把登录失败、邮箱验证、密码重置、管理员代操作或 Worker 任务计为用户活跃。

## 产品口径

- “进入系统”指账号凭有效 Session 成功访问任一受保护页面或 API；成功登录创建 Session 的时刻也计为进入。连续使用同一 Session 时按既有 5 分钟节流更新，避免每个请求都写用户表。
- 账号级 `last_active_at` 保存最近一次进入时间。已有账号从全部历史 Session 的最大 `last_seen_at` 回填，不能从当前有效 Session 列表临时推断。
- 从未进入的 `active` 账号以 `email_verified_at` 作为不活跃计时起点；`pending_email` 尚不能进入系统，`disabled` 被系统禁止进入，两者都不纳入不活跃提醒。
- 只有账号状态为 `active`，且活动基准时间严格早于服务端当前时间减 10 天时才标记为不活跃；恰好 10 天不标记。两种角色均按同一规则展示，`pending_email` 与 `disabled` 仍可显示历史最近进入时间或“从未进入系统”，但 `is_inactive` 必须为 `false`。
- 管理概览显示准确的不活跃账号总数并链接到 `/admin/users?activity=inactive`；用户列表显示最近进入时间或“从未进入系统”，且使用文字提示而非只靠颜色。

## 安全删除边界

- 管理员可对严格超过 10 天未进入的 `active` 账号提交带原因的永久删除；`pending_email`、`disabled` 和未达到阈值的账号均不得通过该入口删除。前端必须二次确认，后端重新锁定 Session 与用户并复核条件，不能仅信任列表状态。
- 禁止删除当前操作者，禁止删除最后一名激活管理员；未达到十天条件返回稳定冲突。
- Session、一次性令牌、站内通知和幂等记录等账号附属数据按既有外键清理；作业受众中的该账号成员关系随账号清理删除，避免仅因自动受众快照使从未提交的账号无法清理。
- 只要账号仍被正式提交、版本、评语、延期、赛事/队伍、上传文件、内容创建/更新、知识库同步或其他需保留记录引用，数据库 `RESTRICT` 继续阻止删除，API 返回“存在需保留业务数据，请改为禁用账号”，不得级联删除正式业务数据。
- 成功和被拒绝的删除尝试均写脱敏审计。物理删除不可由应用回滚，生产执行前继续依赖既有 PostgreSQL 备份；本任务不会自动删除任何现有账号。

## 实施范围

1. 新增可回滚 Alembic `20260827_0012`：为 `users` 增加可空 `last_active_at TIMESTAMPTZ`，从历史 Session 回填；把 `assignment_audience_users.user_id` 外键调整为账号删除时级联，降级恢复 `RESTRICT` 并删除字段。
2. 登录成功时写账号活动时间；认证有效 Session 时与 `sessions.last_seen_at` 共用 5 分钟节流更新。失败认证和公开认证流程不更新。
3. 管理员专用用户响应增加 `last_active_at`、`is_inactive` 和活动天数；`GET /admin/users` 增加受控 `activity=inactive` 过滤，阈值和 `active` 状态条件只由服务端固定计算，登录与 `/auth/me` 响应不增加管理元数据。
4. 新增带 CSRF、管理员鉴权、原因、行锁、最后管理员保护和外键保护的 `DELETE /admin/users/{id}`；Router 只做协议转换，Service 承担规则，Repository 承担持久化与受约束删除。
5. 管理概览增加不活跃账号提醒卡；用户页增加服务端筛选、活跃度展示、风险说明和仅对不活跃账号出现的删除操作。现有“登录人员”页继续表示当前有效 Session，不与历史活跃度合并。
6. 同步产品需求、信息架构、页面、系统架构、API、数据库、安全、测试、决策、变更、基线和任务记录。

## 验证方式

- 后端单元/契约：成功登录更新账号时间；受保护访问按 5 分钟节流更新；失败登录不更新；从未进入、恰好 10 天、超过 10 天和近期进入边界；`pending_email`、`disabled` 排除且 `active` 两角色纳入；管理员过滤准确；删除的允许、非 active、未满十天、当前操作者、最后管理员和受正式数据引用拒绝路径；审计不含敏感信息。
- 数据库迁移：单一 head；`0011 → 0012 → 0011 → 0012` 往返；历史 Session 最大时间回填；受众外键在升级/降级后分别为 `CASCADE`/`RESTRICT`；模型漂移检查通过。
- 前端：管理概览显示准确计数及筛选链接；用户页显示近期、超期、从未进入三态；筛选请求参数正确；删除确认、成功移除和冲突错误可见；资料更新后活跃字段不丢失。
- 质量门：Ruff、格式检查、严格 Mypy、后端测试、ESLint、严格 TypeScript、Vitest 和 Next.js 生产构建；涉及运行态迁移时只在备份或隔离数据库验证，不执行任何账号删除。

## 风险与回滚

- `last_active_at` 是高频但节流的账号字段，不增加 `revision`，避免用户正常浏览导致管理员资料编辑产生虚假版本冲突。
- 10 天提醒是管理视图与筛选，不生成逐日通知或 Outbox，避免重复提醒任务；管理员每次打开概览读取权威实时计数。
- 迁移降级会丢弃升级后新增的账号级活动时间，但历史 Session 仍可在再次升级时回填；已由管理员实际删除的账号只能从备份恢复，这一点必须在确认文案与文档中明确。

## 2026-08-28：账号状态标签 UI 修订

- 管理员用户卡片不再直接显示 `active`、`pending_email`、`disabled`；固定映射为“正常”“待验证”“已禁用”。
- 状态使用圆角胶囊、浅色语义背景、边框、圆点和中文文字，角色使用低饱和蓝灰胶囊；超期提示收敛为带时钟图标的“X 天未登录”。
- 搜索同时纳入中文角色和状态名称；不改变 API 字段、状态机、鉴权、审计、数据库或迁移。
- 验证覆盖三状态呈现、英文枚举不渲染、中文状态搜索、完整前端质量门及隔离运行镜像。

## 2026-08-28 最终整理结果

- 账号后端与 `0012` 已提交为 `b328035`，账号管理前端随共享 UI 提交为 `86a90c4`；未定义 `payload` 已修复，邮箱变更 Session 锁回到正确的 `patch_user()`。
- 账号、认证、问卷、API 契约和迁移定向 52 项通过；最终完整质量门为 218 项后端、20 文件/80 项前端，以及 Ruff、格式、严格 Mypy、ESLint、严格 TypeScript和生产构建。
- Alembic 源码链为 `0011 → 0012 → 0013`，但当前无 Docker socket 权限，`0011 ↔ 0012` 真实 PostgreSQL 往返和运行库应用尚未执行。
- 本任务未调用永久删除接口、未删除现有账号、未部署账号清理后端；运行数据库仍为 `20260827_0011`。
