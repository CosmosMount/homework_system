# 管理员移除作业与通知计划

## 状态

原始删除能力已完成源码实现、完整质量门、隔离候选验证与生产部署。2026-08-31 管理端删除可见性修订已完成源码、完整质量门、隔离迁移往返和生产部署；生产数据库已升级到 `20260831_0017`。
## 2026-08-31 管理端删除可见性修订

### 问题本质

- 原实现用同一个 `archived` 状态同时表示“管理员手工归档、仍可留存查看”和“管理员执行删除、只为保护历史关联而保留根记录”，管理查询无法区分两种意图。
- 管理列表按全部状态查询，所以删除已发布通知或作业后，归档行仍出现在常规管理页面。
- 通知编辑器把整个操作区放在“非归档”条件内，导致手工归档通知无法继续执行删除；作业虽有按钮，但 DELETE 对已归档记录直接幂等返回，也不会产生可供查询隐藏的独立状态。

### 修订方案

1. 在 `announcements` 与 `assignments` 增加可空 `deleted_at TIMESTAMPTZ`，并增加“非空时状态必须为 `archived`”检查约束。`archived_at` 继续表示归档生命周期，`deleted_at` 专门表示管理员删除后的常规管理不可见性。
2. 迁移从成功的 `announcement.delete` / `assignment.delete` 审计回填历史归档删除时间，保证此前已经执行删除的内容上线后立即退出管理页面；手工归档但没有删除审计的内容保持 `deleted_at IS NULL`，仍能进入详情并执行删除。
3. 管理列表和普通管理详情只读取 `deleted_at IS NULL`；学生查询继续按既有状态排除归档。DELETE 内部使用包含已删除行的锁定查询：首次删除手工归档内容时写 `deleted_at`、revision 和脱敏审计，重复删除 `deleted_at IS NOT NULL` 的内容保持 204 幂等。
4. 通知编辑器对手工归档记录继续显示唯一的“删除通知”按钮；保存、发布和更新提醒仍禁用。作业现有归档删除按钮保留。删除成功后返回列表，因后端过滤不会再次显示目标。
5. 不删除提醒、固定受众、提交、版本、评语、优秀标记、附件、文件元数据或审计；本修订只新增两个删除标记与约束，不引入新的业务状态或依赖。

### 迁移与回滚

- 新迁移接在 `20260830_0016` 后，升级新增列、回填历史删除、增加检查约束；降级先删除约束再删除两列，不改归档根记录及其历史关系。
- 降级会失去“手工归档/已删除归档”的结构化区分，使已删除归档内容可能重新进入旧版管理查询；生产回滚必须同时恢复应用与数据库，并在执行过内容删除时优先前滚修复。

### 验证补充

- Repository SQL 覆盖管理列表排除 `deleted_at IS NOT NULL`、普通详情排除已删除行、DELETE 锁定查询可包含已删除行。
- Service 覆盖已发布删除、手工归档后首次删除、重复删除幂等、物理删除草稿和脱敏审计。
- 前端覆盖归档通知只显示删除按钮、确认文案、DELETE 204 后返回列表；作业删除回归保持通过。
- 迁移静态契约覆盖单一 head、两列、两项检查约束、审计回填和反向删除；条件允许时在隔离 PostgreSQL 执行 `0016 → 0017 → 0016 → 0017`。


### 修订执行结果

- 源码已新增迁移 `20260831_0017`、两表 `deleted_at` 与检查约束，Repository/Service/前端操作区及回归测试均按修订方案完成；历史 archive 模式删除审计可回填，手工归档保持可管理并可继续删除。
- 定向后端 45 项、前端 21 项通过；完整后端 320 项、Ruff 172 文件与严格 Mypy 153 个源文件通过，完整前端 25 文件/116 项、ESLint、严格 TypeScript 和 Next.js 生产构建通过。
- 独立临时 PostgreSQL 已完成 `0016 → 0017 → 0016 → 0017`：每类通知/作业各 1 条删除审计记录被回填、各 1 条手工归档记录保持空值，降级删除两列后再次升级结果一致；临时容器已删除。
- 修订已作为应用与 `0017` 同一发布单元上线；生产验收未携带认证信息调用真实 DELETE，PostgreSQL/MinIO 容器和数据卷未重建。

### 修订部署结果

- 部署前 OpenPGP 每日备份 `pnx-backup-20260831T054111Z-daily` 为 99,897,873 字节、0600，SHA-256 `06f7a4657cf53cabdb7c18fe59d1b16974b0d9ad8a31aa0701afc8fa1b1f525b`；PostgreSQL dump 8,157,669 字节、17 版目录 314 项，MinIO 清单 2,939 个对象，本次增量 54 个且删除 0。
- 固定标签 `admin-content-visibility-20260831` 已部署；Backend/Worker 镜像为 `sha256:589290276cd2ab01b283dc227e3effffe9548df4235a8965fa2c2f3c97145772`，Frontend 为 `sha256:8d5ea4a60f04cbf7be44b22b438a1a5c60aef40a0ebf37cf6786ceec2fca116b`，均以 `appuser` 运行。候选 OpenAPI 115 条路径、两项 DELETE/204、前端新旧能力标记和隔离健康均通过。
- 生产迁移前没有成功的通知/作业 archive 删除审计，故历史回填为 0；两条归档通知和一条归档作业保持 `deleted_at=NULL`，作为手工归档留存并可继续删除。两列、两项检查约束和 `20260831_0017 (head)` 均已只读复核。
- 按 Backend/Worker、Frontend/Nginx 两阶段替换后六服务 healthy、重启 0；PostgreSQL/MinIO 容器 ID 未变化。登录和正式健康入口为 200，管理页面匿名 307，管理 API 与虚假 UUID DELETE 匿名 401；运行 OpenAPI、删除过滤与幂等源码、前端确认文案均通过。
- 用户、通知、作业、受众、提交、版本、提醒、文件、知识库和 Outbox 聚合与迁移前一致。问卷回答/选择由外部正常提交从 `182/361` 变为 `183/364`，部署窗口存在两条成功提交审计；部署命令未调用真实业务写接口，四个新应用容器严重错误关键词均为 0。
- 临时候选和备份兼容镜像已清理。Docker socket 的 `user:pnx:rw-` ACL 因 `sudo` 需要交互式密码仍未撤销，部署方需执行 `sudo setfacl -x u:pnx /var/run/docker.sock` 并用 `getfacl -cp /var/run/docker.sock` 复核。

## 背景

管理员当前只能归档已发布通知或已发布/已关闭作业：通知归档后仍可由学生通过历史详情链接读取，作业归档后仍会出现在学生“全部/已截止”列表并可进入详情。草稿和定时内容也没有删除入口，因此管理员无法稳定地把误建或不再投放的内容从学生页面移除。

本任务对应通知域 `NEWS-003`、新增 `NEWS-009`，作业域 `HW-003`、新增 `HW-008`，并继续服从 `SUB-002～SUB-003`、`SHOW-003`、`FILE-003`、`NFR-002`、`NFR-006` 的不可变提交、附件存储、后端授权和审计约束。

## 目标与必要性

- 在通知、作业管理编辑页提供明确的“删除通知 / 删除作业”危险操作，并要求二次确认。
- 未发布内容没有学生历史：通知 `draft/scheduled` 和作业 `draft` 可以在同一事务内物理删除，同时取消仍活动的定时发布任务；通知附件只解除业务绑定，文件按既有孤立对象策略延迟清理。
- 已发布内容可能已有站内提醒、邮件、受众快照、提交版本、评语、优秀标记或附件引用：删除操作不破坏这些历史，而是受审计地转为 `archived`，并立即从学生列表、详情和作业内优秀作业入口隐藏。
- 已归档资源重复删除保持幂等；真实管理员和 CSRF 仍由现有管理依赖验证，管理员学生视图不得调用。

## 方案与影响

### 产品与决策

- 新增 ADR-040，明确“未发布物理删除、已发布归档删除”的统一语义，不新增全局软删除列或第二套状态机。
- `DELETE /admin/announcements/{id}`：草稿/定时通知物理删除；已发布通知归档并把未读公告提醒标为已读；已归档通知直接成功。
- `DELETE /admin/assignments/{id}`：草稿物理删除；已发布/已关闭作业归档；已归档作业直接成功。
- 删除接口统一返回 `204 No Content`。物理删除和归档删除都写动作、原状态与删除模式的脱敏审计。

### 后端

- Router 只增加两个 DELETE 协议入口；Service 负责状态分支、行锁、审计、事务和归档；Repository 负责实体删除与活动定时 Outbox 清理。
- 通知学生详情只允许 `published`；作业学生列表/详情排除 `archived`；优秀作业学生读取同时排除归档作业，真实管理员仍可查看留存记录。
- 不删除 `student_notifications`、已发送邮件 Outbox、提交版本、评语、优秀标记、文件元数据或 MinIO 对象；草稿通知解除附件关联后由既有孤立文件 Worker 清理。

### 前端

- 复用现有 Client Component、`csrfFetch`、`window.confirm` 和 `useRouter.replace`。
- 删除成功后返回对应管理列表；失败保留页面并展示统一 API 错误。
- 已发布内容的确认文案明确“学生端立即隐藏、历史记录保留”，未发布内容明确“永久删除未发布内容”。
- 保留旧归档 API 作为兼容路径，但编辑页不并列展示两个效果重叠的危险操作。

### 数据库与迁移

- 复用既有 `archived` 状态、外键级联和孤立文件清理，不新增字段、枚举、索引或 Alembic 迁移。
- `submissions.assignment_id` 的 `RESTRICT` 与正式版本不可变约束保持不变；已进入学生生命周期的作业不物理删除。

## 验证方式

- 后端服务测试：通知/作业草稿物理删除、活动发布任务取消、审计与提交；已发布内容归档删除；已归档幂等。
- 后端读取测试：归档通知详情 404；归档作业不进入学生列表/详情/优秀作业，真实管理员读取不受影响。
- OpenAPI 契约：两个管理资源路径包含 `DELETE` 且响应为 204；匿名、学生和学生视图继续由管理依赖拒绝。
- 前端组件测试：危险操作确认、DELETE 请求、成功返回管理列表、取消时不请求。
- 定向运行通知/作业/API 契约后端测试和管理员/通知前端测试；再运行 Ruff、格式检查、严格 Mypy、完整 Pytest、ESLint、严格 TypeScript、完整 Vitest，并在条件允许时运行 Next.js 生产构建。

## 风险与回滚

- 未发布内容物理删除不可由应用恢复；界面二次确认，生产操作仍依赖备份。附件对象不会随请求同步删除，可在孤立保留窗口内由备份或运维恢复。
- 已发布内容只做可追溯归档，不破坏学生正式提交；本轮不提供取消归档，误操作需通过受控数据恢复或后续明确需求处理。
- 应用回滚无需数据库降级；回滚后已归档资源可能按旧逻辑重新出现在作业学生列表或通过通知历史详情访问，因此若已使用删除操作，不应只回滚前后端而不同时保持本轮学生查询过滤。

## 执行结果

- 已新增两个返回 204 的管理 DELETE 接口、服务层删除/归档事务、活动定时发布任务清理与脱敏审计；旧归档接口继续兼容。
- 学生通知详情、作业列表/详情、优秀作业及其附件授权均排除已归档资源；管理员历史读取和正式提交数据保持不变。
- 通知和作业编辑器已提供危险操作二次确认，成功后返回管理列表，取消确认不发送请求。
- 最终工作树通过 Ruff、169 文件格式检查、153 个源文件严格 Mypy、264 项 Pytest、ESLint、严格 TypeScript、22 文件/95 项 Vitest 和 Next.js 生产构建；隔离部署候选另通过通知、作业、附件授权和 API 契约 38 项测试、125 个源文件静态检查及容器构建。
- 生产候选明确排除并行的注册唯一约束修复，运行认证源码与部署前一致；Backend/Worker 固定为 `admin-content-removal-20260829`（`sha256:a9e4e73584e35588b537ea1f924c8888a4c73ddd4553ae88aa3ce4322b3f501e`），Frontend 为同标签（`sha256:1ce818d5d2d65cde234e6ce698ca54685ad0def407a4337edad186a375954d5b`）。
- 部署前 PostgreSQL 17 自定义格式备份 `/tmp/pnx-training-before-admin-content-removal-20260829T091428Z.dump` 已通过 `pg_restore --list` 校验，共 314 项、4,320,274 字节、权限 0600，SHA-256 为 `194a1cdd7c2330fc7dd7e0ef70cf7d68240c95761f2c7ad352b5579df4446629`；该文件只适合作为本机短期恢复材料。
- Backend、Worker、Frontend 与 Nginx 已替换，PostgreSQL、MinIO 未重建；六服务 healthy、重启次数为 0，登录页和四个健康端点均为 200，页面/API 匿名守卫为 307/401。运行 OpenAPI 含两个 DELETE/204 路径，Alembic 保持 `20260828_0014 (head)`，本轮无迁移。
- 部署期间真实外部流量使用户从 150 增至 151；问卷、反馈答疑、知识库、Outbox、归档通知/作业和提交计数保持不变，部署命令未调用业务写接口。
- 最近 15 分钟聚合到 6 次既有重复注册唯一约束未映射异常；该问题部署前已存在，正由工作树并行认证修复处理，本次按范围未将其带入生产镜像，不属于管理员内容删除功能回归。
- 应用回滚可恢复 Backend/Worker `auth-window-20260829` 与 Frontend `release-634e01a-20260829`；数据库保持 `0014`。若生产已执行过内容删除，不应单独回滚学生查询过滤，否则归档内容可能重新可见。
