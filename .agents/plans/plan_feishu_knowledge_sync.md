# 飞书知识库同步与培训文档阅读计划

## 背景

当前平台负责新生培训作业与校内赛动态流程，学生仍需要在培训期间查看飞书知识库中的培训文档。同步顺序、块转换和失败语义固定以现有 PNX 官网参考仓库提交 `c28f8a0` 为契约；知识库内容区的初版参考排布已由 ADR-033 定向替代为白色主题、左文档目录、中央正文和右本文目录。本平台继续保留登录态 `AppShell`、受保护 API、成功快照和 MinIO。

本任务不复制或依赖现有官网运行时，也不把飞书变成在线请求依赖。平台只通过飞书开放 API 生成只读快照，学生请求始终读取本地最后一次成功快照。

## 必要性与需求编号

- `KB-001`：登录用户可以查看最近一次成功同步的培训知识库目录与文档。
- `KB-002`：阅读器支持目录搜索、查询参数选中文档、飞书内部文档跳转、桌面/移动目录和本文标题导航。
- `KB-003`：结构化渲染段落、标题、列表、引用、代码、分隔线、提示块、表格、图片、白板和安全附件，不渲染飞书提供的任意 HTML。
- `KB-004`：管理员可以手动创建异步同步任务并查看最近同步状态；同一时刻最多一个同步运行。
- `KB-005`：同步失败不得替换最后一次成功快照；失败信息必须脱敏，前端与日志不得暴露飞书凭证、令牌或原始错误正文。
- `KB-006`：媒体文件存入 MinIO，PostgreSQL 只保存结构化块和媒体元数据；对象键由服务端生成。
- `KB-007`：知识库 API 必须登录鉴权，管理接口必须校验真实管理员角色及 CSRF；管理员学生视图只能访问学生读取接口。
- `KB-008`：外部链接仅允许安全协议；飞书 API 固定可信主机；附件按既有扩展名、媒体类型和大小策略校验，不安全附件只保留飞书原文回退。

## 约束与架构决策

- 新增 ADR，明确本功能取代 ADR-002 中“不复制培训知识库”的绝对限制：允许从飞书同步只读业务快照，但仍禁止修改或依赖现有官网运行时。
- API Router 只负责协议转换、依赖注入与状态码；Service 负责并发同步、快照切换、授权和安全规则；Repository 负责持久化。
- 管理员 `POST /api/v1/admin/knowledge/sync` 只创建同步运行与 Outbox 任务并返回 `202`；常规更新只有 Worker 访问飞书公网，首次上线内部命令可接管唯一活动运行并复用同一同步器。
- 成功快照由最新 `succeeded` 同步运行自然确定，不维护额外可漂移的“当前版本”单例记录。
- 飞书 `app_id`、`app_secret` 和知识库配置来自环境变量或 secret file；不得进入数据库、前端响应或日志。
- 使用结构化 JSON 块并依赖 React 默认文本转义；不信任或渲染任意 HTML。
- 不为推测性增量同步、定时任务、多知识库或公开分享新增抽象。

## 数据库与回滚

新增可回滚迁移 `20260827_0011`：

- `knowledge_sync_runs`：保存 `pending/running/succeeded/failed` 状态、来源、触发管理员、起止时间、文档/媒体计数与脱敏错误；部分唯一索引保证最多一个进行中运行。
- `knowledge_nodes`：保存某次同步的目录树、节点类型、父节点、深度和顺序。
- `knowledge_documents`：保存某次同步的文档标识、标题、来源 URL、结构化块 JSONB 和顺序。
- `knowledge_assets`：保存服务端对象键、受控文件名、媒体类型、大小、哈希和图片尺寸；按飞书资源标识复用。
- `knowledge_document_assets`：保存文档与媒体关系，用于只授权当前成功快照实际引用的资源。

降级时按关联依赖逆序删除上述表与索引，不删除 MinIO 中已经写入的对象；对象清理由应用后续安全回收处理，避免数据库回滚造成不可恢复文件丢失。

## API 影响

- `GET /api/v1/knowledge`：返回当前成功快照元数据、目录树和文档摘要。
- `GET /api/v1/knowledge/documents/{document_id}`：返回当前快照内的文档结构化块。
- `GET /api/v1/knowledge/assets/{asset_id}/content`：鉴权并确认当前快照引用后，跳转到短时 MinIO 签名 URL。
- `GET /api/v1/admin/knowledge`：返回当前快照与最近同步状态，不返回秘密。
- `POST /api/v1/admin/knowledge/sync`：真实管理员鉴权与 CSRF，创建异步同步；已有进行中任务时返回 `KNOWLEDGE_SYNC_IN_PROGRESS`。

## 后端实施

1. 新增 `backend/app/knowledge/` 域，包括模型、Schema、Repository、Service、Router、飞书客户端与同步处理器。
2. 飞书客户端使用固定 `open.feishu.cn` HTTPS 端点、短超时、有限分页和响应体大小；通过可注入传输适配器实现无公网单元测试。
3. 解析知识空间节点与文档块，规范化内部链接；媒体先校验后写 MinIO，危险/超限/失败媒体改为安全回退数据。
4. 扩展 Outbox Processor 识别知识库同步任务，并保证失败状态落库、任务重试幂等及旧成功快照保持可读。
5. 扩展对象存储能力，分别生成受控 inline 与 attachment 短时签名响应。
6. 注册 Router、模型和配置，更新 Compose 与环境变量示例。

## 前端实施

1. 新增 `/knowledge` 登录态阅读页，复用 `AppShell` 与当前蓝白设计令牌。
2. 新增目录树、标题搜索、文档选择、本文目录、移动目录弹层、内部链接转换和原文入口。
3. 新增结构化块渲染器，覆盖参考实现当前支持的文本、列表、表格、代码、图片画廊、白板与附件功能；代码提供语言标签与复制按钮。
4. 新增 `/admin/knowledge` 管理页，展示当前快照、最近运行与手动同步操作；管理员学生视图通过普通 `/knowledge` 阅读。
5. 在共享侧栏增加培训文档入口，在管理员真实视图增加知识库同步入口。

## 安全与失败策略

- 所有读取接口要求有效登录 Session；管理接口使用真实角色，不能被管理员学生视图绕过。
- 对资源下载同时验证“最新成功快照—文档—资源”关系，不接受对象键或任意 URL 参数。
- 外链仅允许 `https`，飞书内部链接只映射已同步文档；未知或不安全链接显示为纯文本或受控回退。
- 同步错误只保存稳定错误码和脱敏说明；不记录 secret、tenant token、Cookie、对象键或飞书响应正文。
- 同步事务仅在整套目录、文档与引用关系就绪后标记成功；失败运行不会成为读取快照。

## 验证方式

- 后端单元/集成测试：飞书鉴权和分页、目录/块解析、内部链接、安全协议、附件白名单与大小、资源授权、管理员权限、CSRF、并发冲突、Worker 成功/失败、旧快照保留、任务重试幂等。
- 迁移测试：从 `20260827_0010` 升级到 `20260827_0011`、降级再升级、模型元数据无漂移。
- 前端测试：阅读器主要块型、搜索与选择、内部跳转、复制、移动目录、空状态/失败状态、管理员同步按钮及学生不可见管理入口。
- 质量门：Ruff、格式、严格 Mypy、Pytest、ESLint、严格 TypeScript、Vitest、Next.js 生产构建。
- 运行态条件允许时：应用迁移、重建 Backend/Worker/Frontend，检查健康端点及登录态知识库页面；真实飞书同步需由部署方配置有效凭证后验收。

## 文档与状态更新

- 更新产品需求、页面说明、架构、API、数据库、安全、部署、测试、ADR 和变更记录。
- 更新 `.agents/memories/project_baseline.md`、`.agents/tasks/current-task.md`，完成后把事项登记到 `.agents/tasks/completed.md`。

## 风险与边界

- 飞书开放 API 的块型和权限可能随租户配置变化；未知块型保留可读占位，不让整次同步因单个展示块失败。
- 大型知识库的首次同步可能耗时较长；任务由 Worker 异步执行并受页数、文档数、媒体数与单文件大小上限保护。
- 无有效飞书凭证时功能仍可部署，但管理员同步会得到明确的安全配置错误，学生看到“尚无成功快照”。
- 本任务不包含定时同步、多人共同编辑、公开访问、全文内容搜索、知识库写回或官网发布。

## 2026-08-27 配置契约修订

- 用户明确要求与参考仓库 `hkustgz-robomaster-pnx.github.io` 对齐，部署方只提供 `FEISHU_APP_ID`、`FEISHU_APP_SECRET` 和 `FEISHU_WIKI_URL`，不再单独填写知识空间 ID 或根节点 token。
- `FEISHU_WIKI_URL` 支持参考实现的两种路径：`/wiki/space/{space_id}` 表示整个知识空间，`/wiki/{node_token}` 表示单个新版文档节点；Worker 从受信 URL 路径解析同步目标。
- 保留旧 `FEISHU_KNOWLEDGE_SOURCE_URL` 的只读兼容别名，便于当前被忽略的 `.env` 无中断迁移；正式示例、Compose 和文档统一使用 `FEISHU_WIKI_URL`，旧 Space ID/Root Token 配置不再参与业务。
- URL 必须是 HTTPS 飞书/Lark 租户域名，不得携带用户名、密码或非标准端口；空间 URL 的 ID 必须为数字，文档 token 必须符合受控字符集。
- 新增配置完整性、空间 URL、文档 URL、非法 URL、空间目录和单文档解析回归；随后重建 Backend/Worker，验证三项配置即可启动、目标可见、健康端点和管理员手动同步。

## 实施结果

- 已完成后端 `knowledge` 域、`sync_knowledge` Outbox 处理、MinIO 媒体导入/签名、五表迁移、五个 API、学生阅读页和管理员同步页。
- 已覆盖旧成功快照保留、当前快照媒体授权、并发触发冲突、管理员学生视图、CSRF 依赖、危险附件、永久失败和重试耗尽。
- 最终质量门通过 29 项后端知识库定向测试、完整后端 213 项测试、Ruff、格式检查和严格 Mypy；前端通过 20 个测试文件/76 项测试、ESLint、严格 TypeScript 和 Next.js 生产构建。
- Alembic 只有 `20260827_0011` 一个 head；`0010 → 0011` 与 `0011 → 0010` 离线 PostgreSQL DDL、开发/生产 Compose 配置通过。
- 已完成 `0010 → 0011` 运行库迁移及 Backend、Worker、Frontend 重建；迁移容器 `Exited (0)`，六个常驻服务 healthy，四个健康端点为 200，学生/管理员知识库页面匿名访问为 307，知识库 API 匿名访问为 401。
- 首次构建因新知识库源码权限为 `0700/0600`、镜像内属主为 root 导致非 root 迁移用户无法导入；数据库未进入迁移事务并保持 `0010`。修正为标准 `0755/0644` 后重建成功。
- 三项真实配置已完成只读诊断：URL 正确解析为整个空间，tenant token 获取成功，空间节点列表读取成功且共 227 个节点；未输出秘密、token、Space ID 或原始响应正文。
- 首次完整租户同步已于 2026-08-27 以前台接管方式完成并验收 `succeeded` 快照、正文块和真实媒体；失败运行不得替换现有成功快照的规则继续有效。

## 2026-08-27 首次上线前台初始化修订

### 背景与必要性

- 首次 227 节点全量同步在 Worker 内串行执行，首轮约 27 分钟后以 `KNOWLEDGE_SYNC_FAILED` 重试，第二轮运行约 20 分钟仍未完成；长任务同时阻塞 Worker 心跳和 Outbox 租约续期，无法满足首次上线速度。
- 用户要求先以前台运维命令完成一次初始成功快照，之后仍由现有管理员手动更新接口创建 Worker 任务；不得新增第二套业务写接口或绕过快照安全规则。

### 实施策略

1. 安全停止正在重试的 Worker，确认不存在并发同步进程；保留当前同步运行和 Outbox 记录用于可追溯接管。
2. 新增内部一次性前台初始化命令，复用 `KnowledgeSynchronizer`、现有配置、数据库和 MinIO 适配器，按最新 active run 接管；命令输出阶段与脱敏进度，不输出凭证、token、Space ID、对象键或飞书响应正文。
3. 前台命令始终复用参考契约同步器：目录和 Docx 串行，附件先处理，图片受 350 ms 共享队列约束，白板直接请求；继续保留文档/媒体总数、单文件大小、受信端点、内容校验和最终原子快照切换。
4. 前台命令成功后恢复 Worker；Worker 重领旧 Outbox 时识别已成功 run 并将任务收敛为 `sent`。现有 `POST /api/v1/admin/knowledge/sync` 及管理员页面继续保留，后续更新仍由 Worker 异步执行。

### 验证方式

- 单元测试覆盖参考同步顺序、前台命令只接管 active run、无 active run 安全失败、完整媒体同步、成功/失败退出码和秘密不输出。
- 完整后端 Pytest、Ruff、格式和严格 Mypy；开发/生产 Compose 解析。
- 运行态核对前台命令完成 `succeeded` 快照、学生知识库目录/文档/媒体可读、Worker 恢复 healthy、旧 Outbox 收敛，以及管理员接口仍返回 `202` 并创建后续 Worker 任务。

本修订不涉及数据库迁移，不改变公开 API 契约、角色授权或学生读取规则。

## 2026-08-27 参考仓库完全复刻修订

### 用户最新要求

- 用户明确要求：若当前自定义提速方案不能完整同步，则同步办法与培训文档内容区页面排布以 `HKUSTGZ-ROBOMASTER-PNX/hkustgz-robomaster-pnx.github.io` 提交 `c28f8a0` 为唯一固定参考，不再自行设计另一套流程或布局。
- 本修订替代上文“正文/媒体受控并发”和“保持当前蓝白内容区排布”的实现选择；管理员手动接口、Worker、登录鉴权、成功快照、MinIO 授权和当前项目全局导航等平台边界继续保留。
- 早期生成的 48 篇正文成功快照曾作为阶段性回退版本；最终 212 篇完整成功快照已经自然替代它，只有完整成功才成为新快照的规则保持不变。

### 同步办法对齐

1. 同步开始显式获取 tenant token；空间目录按参考脚本使用 `page_size=50`、`page_token` 分页和串行 DFS 先序递归，任一目录错误整次失败。
2. Docx 按目录顺序逐篇串行处理；每篇先分页读取 blocks，再请求文档元数据标题。任一正文或标题错误整次失败，不跳过单篇正文。
3. 每篇文档内先发现独立附件和富文本内联附件，再转换块并处理图片、白板；Drive 图片/附件共用串行队列且请求前间隔 350 ms，白板按参考请求发送 `Accept: image/png`。
4. 图片或白板单项失败按参考实现跳过对应块，附件失败显示整篇“在飞书查看”回退；目录、正文和标题失败不得产生部分成功快照。
5. 前台初始化命令复用上述同一同步器并同步媒体；管理员 `POST /api/v1/admin/knowledge/sync` 保持不变，后续仍由 Worker 执行。

### 页面排布对齐

1. 当时的 `/knowledge` 页面范围保留登录态 `AppShell` 外壳并直接复刻参考 `FeishuKnowledgeBase`：顶部仅展示知识库标题和同步统计，不存在文档切换栏；桌面依次为可折叠 280 px 文档目录、位于正文左侧的可折叠 180 px 本文目录和正文；移动端使用右下按钮打开全屏目录。该历史桌面位置与主题已由 2026-08-28 修订和 ADR-033 替代。
2. 块渲染、代码栏/复制、连续图片画廊、表格、提示块、附件、内部飞书链接跳转和空状态按参考组件结构与交互对齐；仅把静态 JSON/公开文件地址替换为当前受保护 API 与资源 UUID。
3. 不复制参考官网的公开访问、硬编码租户域名、静态 token 文件名或全站深色外壳；这些差异仅用于满足本平台鉴权、安全和既有导航边界，不属于自定义知识库排布。

### 验证方式

- 后端回归已覆盖 DFS 顺序、blocks→metadata 顺序、正文 fail-fast、350 ms 资产队列、白板 Accept、内联附件和真实 PostgreSQL 分阶段 flush。
- 前端已按参考组件覆盖桌面/移动布局、文档目录与本文目录折叠、URL 文档选择、内部跳转、代码复制、表格、图片、白板和附件，并断言不存在顶部文档切换栏。
- 完整后端与前端质量门已经通过；定向热修构建没有把并行中的未完成账号清理代码打入运行镜像，账号清理工作树保持原样。
- 真实同步已经得到与参考流程一致的完整成功结果；13 次允许的资源回退按契约处理并记录脱敏信息，未导致运行失败。

本修订不涉及数据库迁移，不删除管理员手动更新接口。

## 最终完成结果

- 2026-08-27 的前台完整同步以 `succeeded` 完成 228 个目录节点、212 篇文档和 977 个成功媒体引用，耗时 `00:33:28.757715`；旧 48 篇阶段性快照已被自然替代。
- 恢复 Worker 后最新 `sync_knowledge` Outbox 为 `sent`、`attempt_count=1` 且无错误；管理员手动更新接口继续保留，后续同步由 Worker 执行。
- Backend/Worker 运行镜像为 `sha256:f8c42b…`，Frontend 为 `sha256:e82d020…`；六服务 healthy，四个健康端点为 200，两个页面匿名访问为 307，两个知识库 API 匿名访问为 401。
- Alembic 保持 `20260827_0011`，本轮无迁移；参考同步办法和当时的桌面/移动知识库内容区排布已上线，页面主题与桌面位置随后由 2026-08-28 修订替代。

## 2026-08-28 白色阅读主题与目录位置修订

### 用户最新要求

- 知识库内容区改为白色主题，取消此前参考页面的深色内容区。
- 桌面保持文档目录在左侧，把本文目录移到正文右侧，形成“文档目录—正文—本文目录”的阅读顺序。
- 用户从文档目录或内部文档链接打开文档后，左侧文档目录自动收起；加载失败时保留当前文档和目录状态，移动端选文档后继续关闭全屏目录。

### 实施范围

1. 仅修改 `frontend/components/knowledge/knowledge-reader.tsx`、`knowledge-blocks.tsx` 及知识库前端测试，不改变同步器、快照、媒体授权、管理员接口或数据库。
2. 白色主题覆盖页面背景、标题、正文、链接、代码、引用、提示块、表格、附件、目录、空状态、错误状态和移动端弹层，保持蓝色作为交互强调色并满足可读对比度。
3. 本文目录桌面栏改到正文右侧；两级目录仍可独立展开/收起，文档目录自动收起后保留可访问的展开按钮。
4. 更新 KB-002、页面说明、设计系统、测试策略、ADR、变更日志、项目基线及任务记录，明确本修订只替代 ADR-032 的主题、本文目录位置和选文档后目录状态，参考同步契约继续有效。

### 验证方式

- 前端组件测试断言白色根主题、桌面 DOM 顺序为文档目录—正文—本文目录、成功打开文档后自动出现“展开文档目录”、失败时当前文档保持可见。
- 运行知识库定向测试、完整 Vitest、ESLint、严格 TypeScript 和 Next.js 生产构建。
- 隔离构建只包含知识库前端及必要共享变更的镜像，替换 Frontend 并重载 Nginx；检查 `/health/live`、`/health/ready`、`/health/worker`、`/nginx-health` 和匿名页面守卫。

本修订不涉及数据库迁移，不重新同步飞书，也不改变管理员手动更新接口。


## 2026-08-28 实施与验收结果

- 已完成白色主题、左文档目录、中央正文、右本文目录，以及成功打开文档后自动收起左目录；失败加载和移动端行为符合修订计划。
- 知识库定向 7 项测试、完整前端 20 文件/76 项测试、ESLint、严格 TypeScript 和 Next.js 生产构建通过。
- 隔离源码从上一版上线知识库基线复制，仅替换 `knowledge-reader.tsx`、`knowledge-blocks.tsx` 和 `knowledge-ui.test.tsx`；账号活跃度/清理工作区差异未进入镜像。
- Frontend 已替换为 `sha256:1842c4d…`，Nginx 已重启；六服务 healthy，四个健康端点为 200，页面/API 匿名守卫为 307/401。
- 最新知识库运行仍为 `succeeded`、212 篇文档、977 个媒体引用，最新 Outbox 仍为 `sent`、`attempt_count=1`；未重新同步飞书，管理员手动接口、Worker、快照和 Alembic `20260827_0011` 不变。

## 2026-08-28 主导航折叠、本文目录跟随与控件统一修订

### 用户最新澄清

- “打开文档后自动收起”指系统最左侧的 `AppShell` 主要导航，不是知识库文档目录；文档目录必须继续保持用户自己的展开/折叠状态。
- 正文右侧的本文目录必须在页面滚动时持续吸附跟随，并突出当前阅读章节；长目录允许在视口内独立滚动。
- 文档目录、目录搜索和展开/收起按键使用系统设计令牌、圆角、边框、阴影、悬停和键盘焦点规范，不保留知识库自定义的直角按钮风格。

### 实施范围

1. 为共享主要导航增加受控的客户端折叠请求；知识库只在当前或新文档成功打开后请求折叠主要导航，失败时不改变主导航状态。
2. 删除成功选文档时对 `sidebarOpen` 的自动修改，保留文档目录自身的手动展开/收起、搜索和移动端关闭行为。
3. 右侧本文目录使用视口吸附、最大高度和内部滚动；监听页面滚动或标题交叉状态，更新 `aria-current=location` 与当前章节视觉状态。
4. 目录行、搜索框、目录标题栏、展开/收起按钮和移动目录弹层改用全局 CSS 令牌及系统紧凑控件规格。
5. 更新 KB-002、设计系统、页面规格、测试策略、ADR、变更记录、运维报告、README、任务记录和项目基线。

### 验证方式

- 组件测试覆盖：成功打开文档后 `AppShell` 主导航变为 `collapsed`，文档目录仍为展开；失败时两者都不改变。
- 通过可控标题位置或交叉观察器验证右侧本文目录当前章节随滚动更新，并断言吸附、最大高度和内部滚动类。
- 断言知识库目录与按键使用系统令牌、圆角和可见焦点样式；保留移动端全屏目录回归。
- 运行知识库定向测试、完整 Vitest、ESLint、严格 TypeScript 和 Next.js 生产构建；从上一版隔离知识库源码基线只叠加本轮知识库/共享导航差异后部署 Frontend。

本修订不涉及同步器、管理员手动接口、Worker、成功快照、媒体授权、Backend 或数据库迁移，不重新同步飞书。


### 实施与验收结果

- 已实现共享主要导航折叠请求；成功打开当前或其他文档后 `AppShell` 主要导航变为 `collapsed`，文档目录仍保持原状态，加载失败不改变两者。
- 右侧本文目录已具备 sticky、最大视口高度、内部滚动和当前章节 `aria-current="location"`；目录控件已统一系统令牌与圆角/焦点样式。
- 知识库定向 8 项测试、完整前端 20 文件/77 项测试、ESLint、严格 TypeScript 和工作树/隔离源码 Next.js 生产构建通过。
- 隔离源码差异仅为 `app-shell-navigation.tsx`、`app-shell-events.ts`、`knowledge-reader.tsx` 和 `knowledge-ui.test.tsx`；账号活跃度/清理差异未进入镜像。
- Frontend 已替换为 `sha256:2ad76bd…`，Nginx 已重启；六服务 healthy，四个健康端点为 200，页面/API 匿名守卫为 307/401。
- 最新知识库运行仍为 `succeeded`、212 篇文档、977 个媒体引用，最新 Outbox 仍为 `sent`、`attempt_count=1`；未重新同步飞书，管理员手动接口、Worker、快照和 Alembic `20260827_0011` 不变。
