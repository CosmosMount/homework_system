# 仓库许可证与分步提交计划

## 状态

已完成。用户确认采用 MIT License，版权主体为 `HKUST(GZ) RoboMaster PNX Team`；现有改动已按六个独立主题完成提交。

## 背景与必要性

- 仓库根目录当前没有 `LICENSE`、`COPYING` 或仓库级 SPDX 声明，第三方依赖锁文件中的许可证字段不能代表本项目的授权条款。
- 当前工作树同时包含持久登录、问卷范围邮件、管理端删除可见性和知识库阅读/目录文件四组已经实现并部署的改动，以及对应测试、迁移和文档记录。
- 用户要求添加仓库许可证并分步提交，因此必须先明确法律授权类型，再按逻辑主题拆分现有改动，避免形成一个混合提交或误纳入秘密、备份和运行数据。

## 影响范围

- 新增仓库根级许可证文件，并在 `README.md` 中增加明确的许可证入口；是否补充包元数据以最终选择的许可证和现有 `private` 配置为准。
- 对现有工作树只做提交整理，不改变已实现功能的产品语义、API、数据库结构或部署状态。
- 继续保留现有迁移顺序 `0016 → 0017 → 0018`，不得重写已经部署的迁移。

## 提交边界

1. 持久登录与可信来源 IP 转发：认证模型、服务、路由、迁移 `0016`、前端登录/会话与回归测试。
2. 问卷范围邮件：问卷 Repository/Service/Router/Schema、Outbox 邮件模板、管理端选择器与测试。
3. 管理端删除可见性：通知/作业删除标记、迁移 `0017`、管理界面和回归测试。
4. 知识库阅读与目录文件：迁移 `0018`、目录文件本地化与授权、默认根层视图、页内图片预览、自适应画廊及真实端点修复。
5. 项目记录：权威需求、架构、页面、API、数据库、安全、测试、ADR、变更、运维、任务、计划与项目记忆的同步记录。
6. 仓库许可证：根级许可证正文和 README 入口；独立提交，便于法律授权单独审阅或回退。

## 实施结果

- 用户已确认采用标准 MIT License，版权声明为 `Copyright (c) 2026 HKUST(GZ) RoboMaster PNX Team`。
- 已按计划完成前五个逻辑提交：
  1. `6e621cc feat(auth): add persistent IP-bound login`
  2. `d620ec4 feat(intentions): reopen surveys and send scoped reminders`
  3. `7d72677 fix(admin): hide deleted archived content`
  4. `912a0bc feat(knowledge): add directory files and responsive media`
  5. `0ce6908 docs: record deployed platform updates`
- 根级 `LICENSE`、README 许可证入口及本次治理记录组成第六个独立提交 `chore: add MIT license`。
- 本任务只整理仓库授权和提交历史，不改变业务功能、API、数据库结构或部署状态，不涉及数据迁移。

## 验证方式

- 核对 `git diff --check`、文件权限、未跟踪文件和暂存区，确认没有 `.env`、证书、备份、生产数据、令牌或生成日志。
- 每次提交前用 `git diff --cached --stat` 和 `git diff --cached --check` 核对单一主题；提交后检查提交文件清单和剩余工作树。
- 复用各正式计划已经完成的完整后端、前端、迁移和部署验证；许可证/README 仅需文本、链接和 Git 差异检查，不重新触发业务写入或生产部署。
- 最终检查提交顺序、工作树是否只剩用户明确保留的改动，并报告提交哈希、测试结果、迁移情况和遗留风险。

## 风险与控制

- 许可证选择会改变第三方使用、修改、再分发和专利授权边界，未经用户确认不擅自选择。
- 当前文档文件包含多项部署记录，若无法无损按功能拆分，则以单独的“项目记录同步”提交承载，代码提交仍严格按功能隔离。
- 不使用 `git reset --hard`、`git checkout --` 或清理命令；现有用户改动全部保留，只通过显式文件清单和暂存区检查创建提交。
