# 2026-08-26：工作台作业可见性与 SVG 图标修复

- 修复 Dashboard 聚合作业查询漏传管理员学生视图临时受众参数；已发布作业现在会出现在学生视图的“近期作业”。
- 新增无第三方依赖的内联 SVG 原子图标，替换共享侧栏、品牌、身份卡、学生视图、退出和抽屉按钮中的首字母/中文单字占位。
- 后端完整 136 项测试、Ruff、格式检查和严格 Mypy 通过；前端 48 项测试、ESLint、严格 TypeScript 和生产构建通过。
- Backend、Worker、Frontend、Nginx 已重建重启，六个 Compose 服务健康，四个健康端点和 5000 端口登录页验收通过。

# 2026-08-26：共享布局与视觉层次细化

- 侧栏导航改为首字母圆角图标和 active 卡片状态，底部增加姓名/有效角色身份卡；退出、学生视图和个人入口统一触控尺寸与 hover 反馈。
- 认证页加入白色表单卡片和 PNX 品牌标识；全局面板阴影、输入框浅蓝表面、标题排版和滚动条细节同步统一。
- 不修改 API、权限、数据库、Markdown 清洗或业务状态机。
- 前端 47 项测试、ESLint、严格 TypeScript 和生产构建通过；Frontend/Nginx 已重建重启，六个服务健康。

# 2026-08-26：纯白背景与整体 UI 优化

- 页面根背景统一为纯白，去掉网格和径向渐变；保留浅蓝交互表面与蓝灰信息层级。
- 统一卡片、认证页、侧栏、管理员页头、表单焦点环和按钮的圆角、边框、阴影与过渡。
- 参考 `../management_system` 的白色表面和轻量层次，不改变 API、权限、数据库或业务逻辑。
- 前端 47 项测试、ESLint、严格 TypeScript 和生产构建通过。
- Frontend 镜像已重建并替换，Frontend/Nginx 已重启；六个 Compose 服务健康，四个健康端点和 5000 端口登录页验收通过。

# 已完成任务

# 2026-08-26：校内赛公告与组队简化

- 新赛事流程聚焦校内赛公告、报名和组队，不再要求赛题即可发布；历史赛题表/API 保留兼容。
- 管理员与学生赛事页面移除赛题入口，补充无赛题发布和公告详情回归测试。
- 后端 130 项测试、前端 45 项测试、Lint、严格类型检查和生产构建通过；Backend/Worker/Frontend 重建、Nginx 重启并完成四个健康端点验收。

# 2026-08-26：侧边栏导航与 Markdown 展示统一

- 将登录后顶部横向导航改为共享响应式侧边栏；桌面端支持折叠，移动端支持抽屉打开/关闭，管理员和学生保持各自的后端权限与入口范围。
- Markdown 编辑表单继续显示源文本；非编辑页面只通过统一 `SafeHtml` 展示服务端清洗后的文档结构，管理员编辑区与用户详情共享相同渲染路径。
- 删除界面中的内部预览标题、清洗标记和保存后预览提示，未保存编辑状态改为简短的内容提示。
- 前端 14 个测试文件、43 项测试、ESLint、严格 TypeScript 和 Next.js 生产构建全部通过；未修改后端 API、数据库或迁移。

# 2026-08-26：管理员命令栏统一与 CI 类型检查修复

- 参考 `management_system` 的 `PageCommandBar` 与按钮尺寸，新增管理员页头组件；通知、作业和赛事列表的创建入口统一为浅蓝、`h-9`、`rounded-lg` 的命令栏按钮链接，保留原有路由、加号图标和管理员后端鉴权。
- 共享 `commandButtonClassName`/`commandLinkClassName` 只用于紧凑命令栏，表单主按钮的 `buttonClassName` 不变；新增回归断言覆盖链接地址、浅蓝令牌、圆角和高度。
- 修复 CI 的 3 个 Mypy 错误：认证测试使用 `AuthenticatedContext` 显式转换，通知测试保留 `RecordingSender` 的具体类型；运行时业务代码未改变。
- 后端定向测试 14 项、严格 Mypy 127 个源文件、Ruff 检查/格式检查，以及前端 14 个测试文件 42 项、ESLint、严格 TypeScript、Next.js 生产构建全部通过。

# 2026-08-25：Markdown 预览与蓝白圆角主题换肤

- 管理员作业编辑页新增“Markdown 渲染预览”，复用后端清洗后的 HTML；新建或未保存时明确提示预览对应最近一次保存版本。
- 全局切换为蓝白配色，增加浅蓝背景、蓝色主操作、蓝灰边框、圆角控件/容器和柔和层次阴影；富文本补充标题、列表、链接、表格、代码块和引用样式。
- 预览继续通过 `SafeHtml` 呈现，不新增前端 Markdown 解析器，不修改业务 API、权限、数据库或迁移。
- 管理员页面回归覆盖预览结构、作业入口、个人资料保存和登录人员视图；前端 14 个测试文件、41 项测试、ESLint、严格 TypeScript 和 Next.js 生产构建全部通过。
- Frontend 已重建并替换，Nginx 已重启；`/health/live`、`/health/ready`、`/health/worker` 和 `/nginx-health` 返回 200，匿名管理员会话接口返回 401。

## 2026-08-25：局域网 HTTP 作业发布兼容修复

- 定位到管理员点击发布时，作业草稿 PATCH 成功但发布 POST 未到达后端；根因是普通局域网 HTTP 属于非安全上下文，浏览器不提供 `crypto.randomUUID()`。
- 新增共享高熵幂等键工具：优先使用原生 `randomUUID()`，缺失时使用仍可用的 Web Crypto `getRandomValues()` 生成 UUID v4；不使用 `Math.random()` 低熵降级。
- 作业发布、通知发布/更新提醒、个人/团队正式版本、上传初始化与完成统一使用该工具，消除同类按钮在 HTTP 验收环境中请求发出前失败的风险。
- 新增工具级和管理员作业发布回归；模拟 `randomUUID` 缺失后仍发出带合法 `Idempotency-Key` 的发布 POST。完整 14 个前端测试文件、40 项测试、ESLint、严格 TypeScript 和主机/容器生产构建通过。
- Frontend 使用新镜像替换，Nginx 已重启；六个常驻服务健康，5000 端口作业编辑页和发布 API 已注册。真实 Chromium 在 `http://10.4.150.222:5000` 验证 `isSecureContext=false`、`randomUUID=undefined`、`getRandomValues=function`。
- 不涉及后端、API 契约、权限、数据库数据或 Alembic 迁移；现有作业内容和状态未被测试流程擅自修改。


## 2026-08-25：历史 Smoke 账号清理与唯一管理员

- 用户确认连带删除全部 Smoke 业务数据后，先生成 PostgreSQL 17 自包含备份和 MinIO 三对象副本，并分别验证备份目录、SHA-256、权限与对象哈希。
- 在 Backend/Worker 维护窗口内以单事务删除 20 个 Smoke 账号、4 条通知、4 个作业、2 个赛事、5 支队伍、2 个提交、3 个正式版本、2 条评语、6 条文件记录、24 条非目标 Outbox 及关联数据；正式版本不可删除触发器只在事务内临时关闭并在提交前恢复。
- 历史审计与认证安全事件保留，已删除用户外键按设计置空；`yzhang367@connect.hkust-gz.edu.cn` 已持久化为唯一的已验证 `active admin`，旧 Session 全部撤销，并新增两条脱敏维护审计。
- MinIO 中 3 个 Smoke 对象在备份后精确删除，桶最终为 0 对象；Alembic 保持 `20260825_0007`。
- 六个常驻服务健康，5000 端口 Nginx、Backend ready 和 Worker 均为 200，管理员路由匿名访问为 307，最近服务日志无严重错误。

## 2026-08-25：唯一账号管理员保证与服务重建

- 用户表恰好一行且该账号已验证、状态为 `active` 时，登录事务会在既有管理员授予 advisory lock 内把真实角色持久化为 `admin`，增加 revision、撤销旧 Session、写 `user.single_account_admin_granted` 审计，再创建管理员 Session；多账号、待验证和禁用账号不会误提升。
- 新增 `20260825_0007` 前滚数据迁移，历史唯一 `active student` 可在部署时立即修正；downgrade 保留已经授予的管理员，避免回滚后重新失去管理入口。ADR-022 替代 ADR-020 对历史唯一账号不自动修正的限制。
- 后端完整 130 项测试、Ruff、136 个 Python 文件格式检查、100 个应用源文件严格 Mypy，以及前端 36 项测试、ESLint、严格 TypeScript 和生产构建全部通过。
- Backend、Worker 和 Frontend 镜像已重新构建并替换，Nginx 已重建；开发 Compose 已到 `20260825_0007`，六个常驻服务健康，`5000` 端口健康、管理员页面守卫及管理员会话 API 鉴权验收通过。
- 部署前后数据库均为 21 个账号（5 个 `admin`、16 个 `student`），不满足唯一账号条件，因此迁移没有改变现有角色、Session 或审计；未擅自删除历史账号。

## 2026-08-25：管理员权限与人员视图补齐

- 核对权限矩阵、作业管理页面和 API 后确认，管理员新建作业能力原本已存在且路由有效；保留 `/admin/assignments/new` 与后端管理员鉴权，并补充页面回归。
- 管理员个人资料页接入现有用户更新接口，可维护自己的姓名、学号和校园邮箱；继续使用 revision、审计及邮箱变更后撤销 Session、重新验证的既有规则，学生资料页保持只读。
- 新增管理员全体活跃登录人员 API 与页面；Repository 只查询未撤销、未过期且用户仍为 `active` 的会话，响应仅包含用户身份、角色、设备摘要、IP 网段和活动时间，不包含 Cookie、Session token 或哈希。
- 后端 128 项测试、Ruff、135 个 Python 文件格式检查、100 个应用源文件严格 Mypy，以及前端 36 项测试、ESLint、严格 TypeScript 全部通过。
- 运行态通过 `5000` 端口复验 Backend live/ready、Worker 心跳、MinIO 存活和 Frontend 路由；三个目标页面匿名访问均 307 到登录页，管理员会话 API 匿名访问返回 401 而不是 404。
- 本次复用现有 `users` 与 `sessions` 表，不涉及数据库迁移。

## 2026-08-25：SMTP 验证邮件受控恢复

- Worker 在解密载荷和连接 SMTP 前复核验证/重置令牌的 ID、用途、未使用状态与有效期；失效任务进入脱敏 `dead/TOKEN_SUPERSEDED`，不会调用发送器。
- SMTP 连接、EHLO、STARTTLS、认证和 QUIT 预检成功，未执行 `MAIL FROM`、`RCPT TO` 或 `DATA`，没有发送测试邮件。
- 停止旧 Worker 后构建并重建 Backend 镜像对应的 Worker；运行容器已加载最新 SMTP 环境。本次恢复窗口只发送 1 条最新有效验证任务，15 条失效任务全部安全终止，未创建额外邮件任务。
- 剩余 2 条历史有效任务的重试时间均晚于令牌过期时间，未来只会被发送前复核终止，不会额外投递。
- 通知定向 8 项测试、Ruff、127 个 Python 文件格式、100 源文件严格 Mypy 和完整 127 项后端测试通过；六个常驻服务健康，只有 Nginx 映射 `0.0.0.0:5000`，Nginx/API 健康返回 200。
- 不涉及公开 API、页面、数据库结构或数据迁移；未输出邮箱、SMTP 凭据、令牌、Cookie 或密文载荷。

## 2026-08-25：重发验证邮件成功空响应修复

- 统一前端 API Client 正确接受 `202`、`204` 等成功空响应，空白正文返回 `undefined`，非空正文继续解析 JSON；错误响应路径不变。
- 新增纯模拟 `202 Accepted` 空响应回归测试，旧实现复现 `Unexpected end of JSON input`，修复后定向测试及完整 32 项前端测试通过。
- ESLint、严格 TypeScript、主机与容器 Next.js 生产构建通过；仅重建并替换 Frontend，六个常驻服务健康。
- 本机及局域网 `10.4.150.222:5000` 的重发验证页面均返回 200；验收只进行页面 GET，没有提交邮箱、调用重发/重置 API 或发送测试邮件。
- 本次不涉及 API 契约变更、后端修改或数据库迁移；部署方确认 SMTP 已配置，但本次未做真实投递验收。

## 2026-08-25：邮箱前缀用户名登录与待验证提示

- Connect 邮箱 local-part 自动作为派生用户名，不新增用户名列；激活账号可用用户名或完整邮箱登录，旧域名存量账号仍使用完整邮箱。
- 登录 API 主字段改为 `identifier` 并兼容旧 `email` 请求；两种 Connect 登录形式统一为同一规范化邮箱、同一限流键和同一账号 Session。
- 注册成功页展示派生用户名并明确邮箱验证完成前不能登录；待验证、禁用、不存在和密码错误继续统一失败，用户名不能绕过验证。
- 新增规范化、API 契约、Service 状态边界和前端交互回归；Ruff/135 文件格式、127 源文件严格 Mypy、123 个后端测试、ESLint、严格 TypeScript、31 个前端测试和主机/容器生产构建全部通过。
- Backend、Worker、Frontend 已用新镜像重建，migrate 退出 0，六服务健康；本机和局域网 `5000` 均为 200，隔离账号以用户名/完整邮箱登录均为 200 且返回同一用户，资源已清理。
- 本次不涉及数据库迁移。开发环境仍有 1 个待验证账号，8 个验证邮件任务因校园 SMTP 未注入而处于 `retry`，需部署方提供真实 SMTP 配置。

## 2026-08-25：Connect 邮箱与空系统初始管理员引导

- 新注册和管理员邮箱修改只接受精确 `@connect.hkust-gz.edu.cn`；旧域名存量账号保持兼容但不自动改写。
- 空系统首个完成邮箱验证的账号在 PostgreSQL 事务锁内唯一成为受最后管理员保护的 `admin`，后续账号保持 `student`；授予写入独立审计，不增加第三角色。
- 新增可回滚 `20260825_0006` 并完成空库升级/降级/再升级；有 Connect 数据时危险降级明确失败并回滚。
- 两个真实并发验证请求均返回 200，结果恰好一个管理员、一个学生、一条授予审计和两个已消费令牌。
- Ruff/格式、严格 Mypy、115 个后端测试、ESLint、严格 TypeScript、30 个前端测试及主机/容器 Next.js 生产构建全部通过。
- 现有 `pnx-training` 已升级至 0006 并重建应用服务；migrate 退出 0，六服务健康，仅 Nginx 映射 `5000`，本机与局域网入口均为 200；真实注册边界通过且虚构账号/邮件任务已清理。

## 2026-08-25：阶段 6 生产运维加固与发布收口实现及验收

- 完成固定镜像生产 Compose、HTTPS Nginx、`*_FILE` secret、非 root/只读根文件系统、最小 capability、资源/PID/日志限制，以及机器健康快照与独立告警规则。
- 完成 OpenPGP 周完整/每日累计增量备份、保留依赖保护、显式隔离恢复和只读 PostgreSQL/MinIO 引用对账；日增量仅归档 1 个/32 B 变化对象，恢复 RPO 31 秒、RTO 13 秒且对账为 0。
- 完成 300 学生/10 管理员容量种子、100 会话读取、20 并发 multipart 和近 2 GiB 单文件；读取 P95 341.754 ms、错误率 0%。
- 完成 Frontend、Backend、Worker、PostgreSQL、MinIO 五组件故障注入与恢复，Worker 在 321 秒准确转为 stale，其他故障边界符合设计。
- 使用固定 Gitleaks 8.30.1 同时扫描 Git 历史和 2.48 MB 当前候选树，泄漏为 0；npm/pip 依赖、Alpine 前后端镜像及生产配置的 HIGH/CRITICAL 均为 0，SBOM 为 Backend 95/Frontend 90 个包。
- Ruff、132 文件格式、125 源文件严格 Mypy、112 个后端测试、ESLint、严格 TypeScript、30 个前端测试和 Next.js 生产构建通过；Chromium/Firefox/WebKit 核心流程及 Chromium 注册写入通过。
- 全新空库完成 `base → 20260825_0005 → base → 20260825_0005`；正式部署脚本生成 `pnx-release-20260825T013516Z`，迁移前后均为 head 且 HTTPS 冒烟通过。
- 本机与 `10.4.150.222:5000` 登录页和健康入口为 200；六个阶段 6 隔离项目及 11 个临时数据卷已删除，`pnx-training` 未被重建或重启并保持六服务健康。

## 2026-08-25：阶段 5 校内赛、报名、队伍与团队提交实现及验收

- 完成赛事创建/编辑/发布/提前关闭/归档和 Worker 六阶段推进，多赛题、个人报名/撤回/取消资格，以及学生/管理员赛事页面。
- 完成邀请码哈希与一次性明文、自助建队/加入/退出/轮换/队长转让/解散、数据库一赛一队约束、报名结束锁队/失效和管理员带原因纠错。
- 完成共享提交的团队所有者、当前队长不可变版本、当前成员版本/评语/下载、历史成员和其他队伍 404、赛事优秀操作拒绝及 `competition_submission` MinIO multipart。
- 完成个人取消资格 API/UI：原因仅本人和管理员可见，不能重报；仍在有效队伍时整队同步取消资格但只显示通用团队原因。
- 新增可回滚 `20260825_0005`，完成阶段 5 → 阶段 4 → 阶段 5 真实迁移往返，最终为单一 head，关键检查、部分唯一索引和团队提交索引已核对。
- Ruff/格式、严格 Mypy、68 个后端测试、ESLint、严格 TypeScript、27 个前端测试、主机及 Docker 内 Next.js 生产构建全部通过。
- 真实局域网 HTTP/MinIO/Worker 冒烟覆盖报名撤回/重报、邀请码轮换、并发一赛一队、自动锁队、管理员补录/移除/队长变更/人数豁免和取消资格、非队长 403、团队上传/提交/评语/下载隐私及归档状态；8 个临时账号均已禁用。
- 六个常驻 Compose 服务健康且只有 Nginx 映射 `0.0.0.0:5000`；`http://10.4.150.222:5000` 的学生和管理员赛事 SSR 路由可访问。

## 2026-08-25：阶段 4 作业、上传与私密评语实现及验收

- 完成作业草稿/定时发布/发布/自动或提前关闭/归档、固定受众快照、公共截止、个人延期、提醒与不含理由的延期邮件。
- 完成个人提交聚合、连续不可变版本、最新版本指针、幂等重放、Markdown/外链/附件组合、跨所有者和跨上下文文件拒绝。
- 完成具体版本私密评语创建与 revision 修订、无正文提醒、管理员提交统计，以及作业内优秀源版本标记/取消和隐私隔离。
- 将通用 MinIO multipart 扩展到作业：恢复、预签名 PUT、完整 SHA-256、附件白名单/总量/状态/占用复核、授权下载和孤立清理保护。
- 完成学生作业列表/详情/版本/优秀版本、正式提交与通用上传器，以及管理员作业编辑、发布、关闭、归档、延期、统计和提交审阅页面；工作台和导航接入真实数据。
- 新增可回滚 `20260824_0004` 迁移并完成阶段 3 → 阶段 4 → 阶段 3 → 阶段 4 往返，最终为单一 head；11 张阶段 4 表与关键约束已核对。
- Ruff/格式、严格 Mypy、59 个后端测试、ESLint、严格 TypeScript、20 个前端测试和 Next.js 生产构建全部通过。
- 真实局域网 HTTP/MinIO/Worker 冒烟覆盖快照、多版本/幂等、上传恢复/校验/下载、越权 404、评语修订、优秀隐私、延期、关闭拒绝、定时发布和自动关闭；六个常驻服务健康且仅 Nginx 暴露 5000。


## 2026-08-24：阶段 3 通知与工作台实现及验收

- 完成安全 Markdown 渲染、全体/届次/方向并集与交集受众、草稿/定时/发布/归档状态、置顶、发布快照、站内未读和显式更新提醒。
- 完成通知发布事务、逐学生唯一提醒、通知邮件 Outbox、Worker 定时发布幂等、SMTP 故障隔离、邮件双格式转义与只含站内详情链接。
- 完成仅开放 `announcement_attachment` 的通用 MinIO multipart：服务端对象键、预签名、恢复、连续分片/ETag/checksum、流式完整 SHA-256、文件签名检测、终止、过期/拒绝/孤立清理和授权下载。
- 完成学生工作台、通知搜索/未读/详情/附件下载，以及管理员概览、通知列表/新建/编辑/预览/上传、邮件任务和审计页面；角色导航与未读角标接入。
- 新增可回滚 `20260824_0003` 迁移并完成阶段 2 → 阶段 3 → 阶段 2 → 阶段 3 往返，最终为单一 head，表和关键约束均与迁移一致。
- 真实验收覆盖草稿不可见、全体/定向、非受众 404、单条/范围已读、Worker 定时、归档、更新提醒幂等、SMTP 不可用不回滚、multipart 上传/下载哈希一致和终态对象清理。
- 修复新 Python 包权限导致镜像内非 root 用户不可导入、异步 ORM 提交后 `updated_at` 触发 `MissingGreenlet`，以及 Nginx 未重载 MinIO 签名 Host 配置的问题并补回归验证。
- Ruff/格式、严格 Mypy、49 个后端测试、ESLint、严格 TypeScript、13 个前端测试和 Next.js 生产构建全部通过；六个常驻 Compose 服务健康，局域网 `http://10.4.150.222:5000` 可访问。

## 2026-08-24：阶段 2 认证与基础数据实现及验收

- 完成精确校园邮箱注册、验证后直接激活、Argon2id、统一登录失败、持久化限流、服务端 Session、CSRF、退出、Session 撤销和一次性密码重置。
- 完成管理员用户查询、禁用/恢复、邮箱/资料/可选分类修改、角色调整、最后管理员保护、届次/方向 CRUD、只读审计、邮件 Outbox 管理与交互式首个管理员命令。
- 完成 AES-GCM 加密的 Outbox 投递秘密、SMTP 双格式邮件、SKIP LOCKED 领取、锁租约、指数退避、dead 状态、错误脱敏、人工重试和 Worker 心跳并行运行。
- 完成真实登录、注册、验证、重发、忘记/重置密码、个人资料、Session、管理员用户及分类页面，并建立集中浏览器 API/CSRF Client 和服务端鉴权 DAL。
- 新增可回滚的 `20260824_0002` 迁移；在真实 PostgreSQL 完成阶段 1 → 阶段 2 → 阶段 1 → 阶段 2，最终约束名称正确且验收账号已清除。
- Ruff/格式、严格 Mypy、37 个后端测试、ESLint、严格 TypeScript、8 个前端测试和 Next.js 生产构建全部通过。
- 真实 HTTP 验证注册/验证/无分类登录、401/403/CSRF、禁用即时失效、密码重置、Session 管理、最后管理员拒绝审计、429 与 `Retry-After`；数据库拒绝非校园邮箱和未验证激活。
- 重建完整 Compose 后六个常驻服务全部健康，Nginx 仅发布 `0.0.0.0:5000`，`http://10.4.150.222:5000` 已返回真实登录页和后端就绪结果。

## 2026-08-24：Linux Docker 阶段 1 集成验收

- 确认 Docker 29.6.0、Docker Compose v5.1.4 和 daemon 可用，完成固定 Python、Node、PostgreSQL、MinIO 与 Nginx 镜像拉取及前后端生产镜像构建。
- 真实启动 PostgreSQL、MinIO、一次性迁移、Backend、Worker、Frontend 与 Nginx；六个常驻服务全部健康，迁移服务按预期以 0 退出。
- 在空 PostgreSQL 数据卷完成 Alembic 升级、降级到 base、再升级，最终版本为 `20260823_0001 (head)`。
- 运行态发现并修复 Nginx 两个请求 ID 正则缺少引号导致的启动失败；`nginx -t`、合法请求 ID 保留与非法值替换均通过。
- 本地 `.env` 以 `0600` 保存随机开发凭证，Nginx 只发布 `0.0.0.0:5000`；本机与局域网 IP 的登录页、存活、就绪和 Worker 健康路径均通过。
- 复核本项目 PostgreSQL、MinIO、Frontend 与 Backend 均无主机端口映射；宿主机已有的 3000/5432 监听属于其他进程或项目，本次未改动。

## 2026-08-23：Windows 本机裸运行验证

- 裸启动 Next.js 与 FastAPI，没有安装或启动 PostgreSQL、MinIO、Nginx、Worker 或 Docker。
- 验证前端 `/login` 200、根路径 307 跳转、`/health` 200、未知页面 404，以及登录表单、邮箱字段和禁用提交按钮结构。
- 验证后端存活 200、有效 `X-Request-ID`、统一 404；未启动 PostgreSQL 时就绪接口返回不泄露连接信息的 503。
- 裸测发现并修复 DNS/套接字错误未映射为依赖不可用的问题，新增两项回归测试，后端测试总数为 14。
- 本地浏览器自动化连接受 Windows 沙箱限制，未生成视觉截图；页面已通过 HTTP、组件测试、类型检查和生产构建验证。

## 2026-08-23：阶段 1 工程骨架实现

- 建立 Next.js 16 + React 19 严格 TypeScript 前端、PNX 深色登录骨架、健康 Route Handler 和前端测试。
- 建立 FastAPI Router → Service → Repository 后端、统一错误、请求 ID、结构化日志、异步 SQLAlchemy 会话和健康 API。
- 建立数据库心跳 Worker、容器健康探针及可回滚的首条 `worker_heartbeats` Alembic 迁移，未提前创建业务表。
- 建立固定版本的前后端 Dockerfile、PostgreSQL、MinIO、Nginx、一次性迁移服务、持久卷和三网络 Compose 拓扑；只有 Nginx 映射主机端口。
- 建立前后端 lint、格式、类型、测试、生产构建、依赖漏洞审计、真实 PostgreSQL 迁移和 Compose 冒烟 CI。
- 本机通过 Ruff、格式、Mypy、14 个后端测试、ESLint、TypeScript、3 个前端测试、Next.js 生产构建、`pip-audit`、`npm audit` 及静态配置交叉断言。
- 当前主机未安装 Docker，实际镜像构建、Nginx 语法、空库迁移和完整 Compose 冒烟保留为当前集成验收任务。

## 2026-08-23：简化注册与优秀作业设计

- 将注册流程改为精确校验 `@hkust-gz.edu.cn`，邮箱验证后直接激活，不再经过管理员审核或强制初始分组。
- 保留届次与技术方向作为管理员事后维护的可选分类；未分类用户可正常登录并接收全体内容。
- 删除独立优秀范本与赛事范本，改为在对应作业中标记并展示不可变个人作业版本。
- 同步更新需求、页面、架构、API、数据库、安全、上传、部署、测试、路线图、ADR、变更记录、任务和项目记忆。
- 自动检查 25 份 Markdown、17 份编号设计文档和 75 个需求编号，链接、围栏、未决标记、需求追溯、测试覆盖与关键语义问题均为 0。
- 本次只调整权威设计文档，未生成业务源码、迁移或部署配置。

## 2026-08-23：权威设计文档基线

- 建立根目录 README、AGENTS 和中文任务计划。
- 完成 17 份产品、交互、技术、安全、部署、测试、工程与决策文档。
- 定义 75 个需求编号，并建立页面、API、数据实体和测试场景追溯矩阵。
- 自动检查 24 份 Markdown：本地链接、代码围栏、中文内容、未决标记、行尾格式、文档编号、需求追溯和测试映射问题均为 0。
- 本里程碑按范围未生成业务源码、迁移、依赖或部署配置。

## 2026-08-26：管理员临时学生视图

- 实现管理员当前 Session 的“查看学生视图/返回管理员视图”，保留真实 `admin` 角色，仅将有效角色按 Session 标记切换为 `student`；学生视图可读取和操作学生工作台、通知、作业、赛事与个人提交。
- `AdminContextDependency` 和管理服务拒绝学生视图的管理员 API；其他管理员真实降级为 `student` 时撤销全部 Session，本人重新登录后不能自助恢复管理员权限。
- 新增可回滚 `20260826_0008` 迁移、认证/用户管理/前端导航回归，更新需求、页面、架构、API、数据库、测试、决策与变更记录。
- 后端 134 项测试、Ruff、严格 Mypy；前端 47 项测试、ESLint、严格 TypeScript、Next.js 生产构建通过。新镜像已构建，迁移已应用到 `20260826_0008`，Backend/Worker/Frontend/Nginx 已重启，六服务和 5000 端口健康端点均通过。


## 2026-08-26

- 移除届次相关产品设置与新建受众选择，保留历史 API/数据兼容；方向设置、用户资料、通知和作业页面完成收敛。


## 2026-08-27：单一校内赛管理入口与管理员页头统一

- 管理员所有页面统一使用共享页头；校内赛管理首页聚焦当前赛事和队伍列表，隐藏多赛事创建入口。
- 新增服务层重复校内赛保护，保留历史归档数据与兼容赛题 API。
- 前端、后端回归及静态质量门通过。


## 2026-08-27：学生校内赛队伍中心与意向调查

- 学生赛事入口收敛为单一当前校内赛队伍中心；公开目录只列未满 `forming` 队伍的名称、状态和人数，支持搜索、分页、邀请码加入和主动自动分配。
- 自动分配在事务内优先加入人数最少队伍，无候选时自动建队并一次返回邀请码；一赛一队唯一索引继续处理并发冲突。
- 新增登录态单选/多选意向调查、本人回答修改、管理员匿名汇总和本地二维码；token 只存 SHA-256，轮换后旧码失效，扫码仍需登录。
- 新增可回滚迁移 `20260827_0009` 并完成隔离库 `0008 → 0009 → 0008 → 0009`；运行库为 `0009 (head)` 且 Alembic 无模型漂移。
- Ruff、145 个 Python 文件格式、134 个源文件严格 Mypy、159 项后端测试、ESLint、严格 TypeScript、18 个前端测试文件/62 项测试、主机/容器生产构建及两端依赖审计全部通过。
- Backend、Worker、Frontend 和 Nginx 已重建重启；六个 Compose 服务 healthy，四个健康端点为 200，受保护页面/API 匿名访问分别为 307/401。
