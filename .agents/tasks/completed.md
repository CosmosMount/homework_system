# 已完成任务

## 2026-08-23：Windows 本机裸运行验证

- 裸启动 Next.js 与 FastAPI，没有安装或启动 PostgreSQL、MinIO、Nginx、Worker 或 Docker。
- 验证前端 `/login` 200、根路径 307 跳转、`/health` 200、未知页面 404，以及登录表单、邮箱字段和禁用提交按钮结构。
- 验证后端存活 200、有效 `X-Request-ID`、统一 404；未启动 PostgreSQL 时就绪接口返回不泄露连接信息的 503。
- 裸测发现并修复 DNS/套接字错误未映射为依赖不可用的问题，新增两项回归测试，后端测试总数为 14。
- 本地浏览器自动化连接受 Windows 沙箱限制，未生成视觉截图；页面已通过 HTTP、组件测试、类型检查和生产构建验证。

## 2026-08-23：阶段 1 工程骨架实现

- 建立 Next.js 16 + React 19 严格 TypeScript 前端、PNX 深色登录骨架、健康 Route Handler 和前端测试。
- 建立 FastAPI Router → Service → Repository 后端、统一错误、请求 ID、结构化日志、异步 SQLAlchemy 会话和健康 API。
- 建立数据库心跳 Worker、容器健康探针及可回滚的首条 `worker_heartbeats` Alembic 迁移，未提前创建业务表。
- 建立固定版本的前后端 Dockerfile、PostgreSQL、MinIO、Nginx、一次性迁移服务、持久卷和三网络 Compose 拓扑；只有 Nginx 映射主机端口。
- 建立前后端 lint、格式、类型、测试、生产构建、依赖漏洞审计、真实 PostgreSQL 迁移和 Compose 冒烟 CI。
- 本机通过 Ruff、格式、Mypy、14 个后端测试、ESLint、TypeScript、3 个前端测试、Next.js 生产构建、`pip-audit`、`npm audit` 及静态配置交叉断言。
- 当前主机未安装 Docker，实际镜像构建、Nginx 语法、空库迁移和完整 Compose 冒烟保留为当前集成验收任务。

## 2026-08-23：简化注册与优秀作业设计

- 将注册流程改为精确校验 `@hkust-gz.edu.cn`，邮箱验证后直接激活，不再经过管理员审核或强制初始分组。
- 保留届次与技术方向作为管理员事后维护的可选分类；未分类用户可正常登录并接收全体内容。
- 删除独立优秀范本与赛事范本，改为在对应作业中标记并展示不可变个人作业版本。
- 同步更新需求、页面、架构、API、数据库、安全、上传、部署、测试、路线图、ADR、变更记录、任务和项目记忆。
- 自动检查 25 份 Markdown、17 份编号设计文档和 75 个需求编号，链接、围栏、未决标记、需求追溯、测试覆盖与关键语义问题均为 0。
- 本次只调整权威设计文档，未生成业务源码、迁移或部署配置。

## 2026-08-23：权威设计文档基线

- 建立根目录 README、AGENTS 和中文任务计划。
- 完成 17 份产品、交互、技术、安全、部署、测试、工程与决策文档。
- 定义 75 个需求编号，并建立页面、API、数据实体和测试场景追溯矩阵。
- 自动检查 24 份 Markdown：本地链接、代码围栏、中文内容、未决标记、行尾格式、文档编号、需求追溯和测试映射问题均为 0。
- 本里程碑按范围未生成业务源码、迁移、依赖或部署配置。
