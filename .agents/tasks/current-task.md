# 当前任务

## 2026-08-28：高并发、局域网安全与 Docker 版本收敛（已完成）

- 保留单 IP 认证限流，将 Nginx 超限响应从默认 503 修正为 429；单 IP 30 账号验证为 21 成功、9 个预期 429。
- 隔离多来源 Docker 登录 100/200/300 账号全部成功；100 Session、2,000 次登录后读取 P95 331.674 ms、错误率 0%。
- 本机 PNX 入口的鉴权、转发头、Origin/CSRF、输入边界、安全头和 source map 探测均通过；Gitleaks 无泄漏，Trivy 的 4 个基础镜像 CVE 已记录待修复。
- 仅删除 PNX 旧镜像标签、已退出迁移容器和隔离压测资源；当前/回滚镜像、服务、卷、备份和既有未跟踪文件均保留。
- 唯一正式计划：`.agents/plans/plan_concurrency_lan_security_hardening.md`。

## 状态

已完成：Dashboard 作业可见性修复、SVG 原子图标统一、Markdown 展示统一、蓝白圆角主题换肤、纯白背景与共享布局视觉细化、管理员命令栏统一、侧边栏导航及 CI 类型检查修复。

## 完成内容

- 管理员作业编辑页保留 Markdown 源文本框，保存后的说明通过与用户详情相同的安全 HTML 渲染展示，不暴露内部实现标签。
- 全局切换为蓝白配色，使用纯白页面背景、浅蓝交互表面、蓝色主操作、蓝灰边框、圆角控件/容器和柔和层次阴影。
- 富文本预览补充标题、列表、链接、表格、代码块和引用样式，继续通过 `SafeHtml` 呈现，不新增前端 Markdown 解析器。
- 增加管理员页面回归测试，覆盖预览结构、作业入口、个人资料保存和登录人员视图。

## 验证结果

- 前端 14 个测试文件、43 项测试通过；ESLint、严格 TypeScript 和 Next.js 生产构建通过。
- Frontend 已重建并替换，Nginx 已重启；`/health/live`、`/health/ready`、`/health/worker` 和 `/nginx-health` 均返回 200。
- 匿名访问 `/api/v1/auth/admin/sessions` 返回 401；本次未修改业务 API、权限、数据库或迁移。

## 本次追加：按钮视觉优化

- 主操作按钮改为浅蓝底深蓝字，统一圆角、阴影和悬停反馈；保留深蓝状态标签以维持对比度。
- 学生端筛选/上传，以及管理员新建通知、新建作业、新建赛事入口接入共享按钮样式。
- 管理员创建入口增加加号图标和清晰的按钮语义。
- 管理员通知、作业和赛事列表页统一复用紧凑命令栏，创建链接使用浅蓝 `h-9`/`rounded-lg` 按钮样式，表单主按钮保持原有尺寸。
- 修复 `test_auth_login.py` 的 `AuthenticatedContext` 测试类型标注，以及 `test_notifications.py` 的 `RecordingSender` 属性推断；不改变运行时逻辑。

## 本轮验证结果

- 后端定向认证/通知测试 14 项、严格 Mypy（127 个源文件）、Ruff 检查与格式检查通过。
- 前端 14 个测试文件 42 项测试、ESLint、严格 TypeScript 和 Next.js 生产构建通过。

## 本次追加：侧边栏与统一 Markdown 展示

- 顶部横向导航改为响应式侧边栏：桌面端可折叠，移动端使用抽屉入口；管理员和学生共享布局组件并保持各自的权限入口。
- Markdown 源文本仅在编辑表单中可见；通知、作业、赛事和提交详情统一展示服务端清洗后的安全 HTML，管理员编辑区与学生详情复用同一渲染组件。
- 删除编辑区中的内部实现标签和预览标题，保留必要的“保存后可查看内容”状态提示。

## 本轮验证结果

- 前端 14 个测试文件、43 项测试通过；ESLint、严格 TypeScript 和 Next.js 生产构建通过。

## 本次追加：作业源码默认收起

- 管理员作业编辑页对已有安全 HTML 的作业先展示渲染结果，Markdown 源文改为通过“编辑 Markdown 源文”按需展开；新建或尚未生成 HTML 的草稿仍默认显示编辑框。
- 用户详情页和管理员已保存内容继续复用同一 `RenderedMarkdown`/`SafeHtml` 渲染路径，未修改 API、权限或数据结构。

## 本轮验证结果

- 管理员界面回归测试 7 项、完整前端 14 个测试文件 43 项测试、ESLint、严格 TypeScript 和 Next.js 生产构建均通过。
- Frontend 镜像已重建并替换，Frontend 与 Nginx 已重启；Backend、Worker、PostgreSQL、MinIO 保持健康。
- `/health/live`、`/health/ready`、`/health/worker`、`/nginx-health` 均返回 200；匿名访问 `/admin/assignments/new` 正确重定向到 `/login`。

## 本次追加：纯白背景与整体 UI 优化

- 页面根背景和 HTML 根节点改为纯白，移除网格/径向渐变，避免影响通知、作业与 Markdown 阅读。
- 调整蓝灰色令牌、浅蓝主按钮、输入焦点环、卡片阴影、侧栏和认证页圆角，统一页面层级并保持管理员与学生共用布局。
- 参考 `../management_system` 的轻边框、白色表面和紧凑层次，不修改业务 API、权限、数据库或 Markdown 清洗。

## 本轮验证结果

- 前端 14 个测试文件、47 项测试、ESLint、严格 TypeScript 和 Next.js 生产构建通过。
- Frontend 已重建并替换，Frontend/Nginx 已重启；六个 Compose 服务健康，/health/live、/health/ready、/health/worker、/nginx-health 和 5000 端口登录页均返回 200。


## 本次追加：共享布局与视觉层次细化

- 共享侧栏改用首字母圆角导航图标与 active 卡片状态，底部增加姓名和有效角色身份卡；退出、学生视图、个人资料和登录设备入口统一触控尺寸与交互反馈。
- 认证页增加白色圆角表单卡片和 PNX 品牌标识；面板、输入控件、标题排版与滚动条细节统一为纯白背景下的蓝白视觉层次。
- 不修改 API、权限、数据库、Markdown 清洗或业务状态机。

## 本轮验证结果

- 前端 14 个测试文件、47 项测试、ESLint、严格 TypeScript 和 Next.js 生产构建通过。
- Frontend/Nginx 已重建重启；Backend、Worker、PostgreSQL、MinIO、Frontend 和 Nginx 六个 Compose 服务健康，健康端点与 5000 端口登录页均返回 200。

## 正式计划

- .agents/plans/plan_markdown_preview_blue_white_theme.md
- .agents/plans/plan_button_visual_polish.md
- .agents/plans/plan_sidebar_markdown_rendering.md
- .agents/plans/plan_assignment_editor_visibility.md
- .agents/plans/plan_admin_student_view.md
- .agents/plans/plan_ui_visual_refinement.md

## 本次追加：赛事公告与组队简化

- 新赛事仅保留校内赛公告、报名、建队和邀请码组队；管理员与学生页面移除赛题创建、编辑、列表和提交入口。
- 发布赛事不再要求至少存在一个赛题；保留历史赛题表和 API 作为兼容路径，不做数据库迁移或删除。
- 新增后端无赛题发布测试和学生公告型赛事详情回归测试；同步产品、页面、API、架构、数据库、测试、决策和变更文档。

## 验证结果

- 后端完整测试 130 项、Ruff、格式检查、严格 Mypy（127 个源文件）通过。
- 前端完整测试 14 个文件/45 项、ESLint、严格 TypeScript、Next.js 生产构建通过。
- Backend、Worker、Frontend 已重建并重启，Nginx 随后重启；`/health/live`、`/health/ready`、`/health/worker`、`/nginx-health` 均返回 200。
- 5000 端口匿名赛事与管理员入口均正确进入登录守卫；旧赛题路径不暴露受保护内容。

## 状态

已完成：校内赛赛事已收敛为公告、报名和组队流程。

## 本次追加：管理员临时学生视图

- 管理员可在当前 Session 通过侧栏切换为学生视图并操作学生工作台；关闭后回到管理员工作台，真实 `users.role` 不变。
- 学生视图使用 `sessions.student_view` 和有效角色授权；管理 API（含登录人员、用户、通知、作业、赛事管理）由后端拒绝，学生通知/作业/赛事/个人提交/上传按学生权限执行。
- 其他管理员将账号真实降为 `student` 时撤销该用户全部 Session，本人重新登录后不能通过学生视图按钮恢复管理员权限。
- 新增 `20260826_0008` 可回滚迁移、认证/用户管理和前端侧栏回归测试，并同步需求、页面、架构、API、数据库、测试、决策和变更文档。

## 当前状态

已完成：实现、定向与完整质量门、迁移应用、镜像重建、服务重启和 5000 端口健康验收均通过。

## 本次追加：管理员学生视图作业受众预览

- 修复管理员开启学生视图后作业列表为空：发布时受众快照只包含普通学生，唯一管理员不会被写入快照。
- 学生视图现按管理员账号当前届次/方向在服务端临时预览已发布作业；普通学生仍使用固定快照。提交、上传、优秀作业及附件下载共用该预览授权。
- 预览不写入受众快照、站内通知、邮件或统计数据。

## 本轮验证结果

- 后端 135 项测试、Ruff、格式检查和严格 Mypy 通过；新增作业受众预览回归。
- 运行数据库确认唯一管理员、已发布作业及受众快照数量；待重建镜像后再做运行态 API 验收。


## 本次追加：工作台作业可见性与 SVG 原子图标修复

- Dashboard 聚合作业查询补传管理员学生视图的临时受众参数；发布时未写入普通学生快照的作业现在会显示在工作台“近期作业”。普通学生仍遵循固定受众快照，未修改受众、权限或数据库结构。
- 共享侧栏、品牌标识、身份卡、学生视图、退出和抽屉按钮改用统一内联 SVG 原子图标，保留可读中文标签与无障碍名称，不引入第三方依赖。

## 本轮验证结果

- 后端完整 136 项测试、Ruff、格式检查和严格 Mypy 通过；前端 48 项测试、ESLint、严格 TypeScript 和 Next.js 生产构建通过。
- Backend、Worker、Frontend、Nginx 已重建重启；六个 Compose 服务健康，健康端点与 5000 端口登录页均返回 200。

## 本次追加：删除届次相关设置

- 管理员侧栏与分类页改为“方向设置”，删除届次创建/启停入口。
- 用户管理、个人资料、工作台和通知说明不再展示届次；用户编辑仅维护技术方向。
- 新建/编辑通知与作业受众改为全部学生或按技术方向，历史届次受众 ID 原样兼容保留。
- 保留数据库、旧 `/admin/cohorts` API 和历史关联，不做破坏性迁移或数据清理。

## 本轮验证结果

- 前端 14 个测试文件、50 项测试通过；ESLint、严格 TypeScript 和 Next.js 生产构建通过。
- 后端完整 136 项测试、Ruff、格式检查和严格 Mypy 通过。
- Backend、Worker、Frontend、Nginx 已重建重启，六个 Compose 服务健康；`/health/live`、`/health/ready`、`/health/worker`、`/nginx-health` 和 5000 端口登录页均返回 200。


## 本次追加：单一校内赛管理入口与管理员页头统一

- 通知、作业、赛事及其它管理员页面统一使用 `AdminPageHeader`，详情页增加一致的返回入口。
- 管理员侧栏与赛事管理标题统一为“校内赛”；首页直接展示当前未归档校内赛及队伍卡片，不再显示“新建赛事”。
- 首次配置路由仅在不存在当前赛事时可用；后端 `CompetitionService` 在创建前检查未归档赛事并返回 `CAMPUS_COMPETITION_EXISTS`，归档历史数据和兼容赛题接口保留。
- 新增前端校内赛管理回归与后端重复创建冲突回归。

## 本轮验证结果

- 前端 15 个测试文件、51 项测试、ESLint、严格 TypeScript 通过。
- 后端赛事定向测试 10 项、完整 136 项测试、Ruff、格式检查和严格 Mypy 通过。
- 生产构建、Compose 镜像重建、服务重启和运行态健康验收均已完成。六个服务 healthy，健康端点和 5000 端口登录页返回 200；管理员路由匿名访问按预期返回 307。


## 本次追加：学生校内赛队伍中心与意向调查

- 学生 `/competitions` 收敛为当前校内赛队伍中心，支持未满成形队伍的名称搜索、分页、邀请码加入和本人主动自动分配；公开目录不返回成员或邀请码。
- 新增登录态意向调查域：管理员创建单选/多选、推进状态、查看匿名汇总并生成本地二维码；学生只读取和修改本人回答。
- 新增可回滚 `20260827_0009` 四表迁移，二维码 token 只存 SHA-256，登录 `next` 只接受安全同源路径。

- 正式计划：`.agents/plans/plan_student_teams_auto_assignment_intentions.md`。

## 最终状态

已完成：完整质量门、隔离迁移往返、运行库升级、镜像重建、服务重启和 5000 端口验收全部通过。后端 159 项测试，前端 18 个文件/62 项测试；六个 Compose 服务 healthy，Alembic 为 `20260827_0009 (head)` 且无模型漂移。当前没有进行中的仓库开发任务。

## 本次追加：密码最小长度调整为八位

- 注册、密码重置和管理员命令行创建账号统一接受 8～128 个 Unicode 字符的密码。
- 保留 Argon2id、128 位上限、常见密码拒绝及邮箱/学号相似性检查，并补充常见 8 位弱密码。
- 新增 7、8、128、129 位服务端边界测试，以及注册和重置表单 8 位浏览器约束回归。
- 正式计划：`.agents/plans/plan_password_minimum_eight.md`。

## 最终状态

已完成：后端 163 项测试、Ruff、格式检查和严格 Mypy，前端 18 个文件/63 项测试、ESLint、严格 TypeScript 及主机/容器生产构建均通过。Backend、Worker、Frontend 和 Nginx 已重建重启；六个 Compose 服务 healthy，四个健康端点、注册页和重置页均返回 200，运行容器已确认接受安全 8 位密码。本次不涉及 API 结构、数据库或数据迁移。


## 本次追加：后注册学生作业可见性修复

- 根因是“电控第一次作业”发布时没有普通学生，`xluo799@connect.hkust-gz.edu.cn` 两天后激活，固定受众快照仍为 0。
- 后续普通学生完成邮箱验证时，激活事务会把仍为 `published`、未过截止且匹配的作业追加到正式快照；首个管理员和不匹配/已关闭作业不加入。
- `20260827_0010` 已为现有 active student 补录开放作业，学生现已进入该作业正式受众，目标人数为 1。
- 正式计划：`.agents/plans/plan_late_student_assignment_audience.md`。

## 最终状态

已完成：后端 164 项测试、Ruff、146 个 Python 文件格式检查和严格 Mypy，前端 18 个文件/63 项测试、ESLint、严格 TypeScript 与生产构建均通过；隔离迁移往返、运行库升级、镜像重建、服务重启和运行态数据验收完成。六个 Compose 服务 healthy，四个健康端点为 200，Alembic 为 `20260827_0010 (head)` 且无模型漂移。

## 本次追加：飞书培训知识库同步与阅读

- 新增管理员手动 Outbox 同步、Worker 飞书客户端、版本化知识库目录/文档/媒体快照、当前成功快照资源授权和 MinIO 受控媒体。
- 新增学生 `/knowledge` 阅读器与管理员 `/admin/knowledge` 状态页，覆盖目录搜索、内部跳转、本文目录、移动抽屉、代码复制、表格、图片、白板和附件。
- ADR-031 部分替代“不复制知识库”的旧限制；根规则、README、项目基线及所有受影响的需求、页面、设计、架构、API、数据库、安全、存储、部署、测试、路线图、决策和变更文档均已同步。
- 正式计划：`.agents/plans/plan_feishu_knowledge_sync.md`。

## 初版实现与部署状态

知识库初版仓库实现与运行态部署已完成：后端 179 项测试、Ruff、157 个 Python 文件格式检查和 144 个源文件严格 Mypy 通过；前端 19 个文件/67 项测试、ESLint、严格 TypeScript 和 Next.js 生产构建通过。开发/生产 Compose 配置、Alembic 单 head 及 `0010 ↔ 0011` 离线 PostgreSQL DDL 均通过。修正新知识库源码 `0700/0600` 导致镜像内非 root 用户不可读的问题后，Backend、Worker、Frontend 已重建并启动，迁移容器 `Exited (0)`，运行库为 `20260827_0011 (head)`；六个常驻服务 healthy，四个健康端点为 200，知识库页面/API 匿名守卫为 307/401。

## 本次追加：飞书三项配置契约对齐

- 部署方只需提供 `FEISHU_APP_ID`、`FEISHU_APP_SECRET`（生产可用 secret file）和 `FEISHU_WIKI_URL`；删除正式生产示例中的旧 Space ID/Root Token 字段。
- `/wiki/space/{数字ID}` 自动同步整个空间，`/wiki/{node_token}` 自动解析并同步单篇新版文档；旧 `FEISHU_KNOWLEDGE_SOURCE_URL` 仅作无中断兼容别名。
- 真实只读诊断已成功取得 tenant token 并读取整个空间 227 个节点，未输出凭证、token、Space ID 或飞书原始响应。
- 该配置对齐阶段通过后端 179 项测试、Ruff、157 个 Python 文件格式检查、144 个源文件严格 Mypy 和开发/生产 Compose 解析；四个运行健康端点再次返回 200。

## 当前追加：首次上线前台初始化

- 原管理员任务首轮约 27 分钟后以 `KNOWLEDGE_SYNC_FAILED` 重试，第二轮约 20 分钟仍未完成；随后安全停止旧 Worker，并以前台内部命令接管唯一活动运行。
- 前台初始化已生成 228 个目录节点、212 个 Docx 目标、48 篇可读正文和 0 个首轮媒体的回退快照；恢复 Worker 后原 Outbox 已收敛为 `sent`，四个健康端点为 200。
- 正式计划继续使用 `.agents/plans/plan_feishu_knowledge_sync.md`，新增前台初始化修订，不建立第二份计划。
- 内部命令复用同一 `KnowledgeSynchronizer`，不新增公开 API；管理员 `/admin/knowledge` 手动更新接口保持不变，后续更新继续由 Worker 执行。

## 当前状态

该阶段已完成前台初始化与 Worker 恢复；48 篇快照当时先供学生回退读取，随后已由参考契约的 212 篇完整成功快照替代。本次不涉及数据库迁移。

## 本次追加：按参考仓库完全复刻知识库同步与页面排布

- 前台初始化已在 32 秒内原子生成首个成功快照：228 个目录节点、212 个 Docx 节点、48 篇可读正文、0 个首轮媒体；对应 Outbox 已由恢复后的 Worker 收敛为 `sent`，四个健康端点为 200。
- 48 篇结果不作为最终完整验收。用户最新要求改为以参考仓库提交 `c28f8a0` 完全复刻同步顺序、失败策略、资源节奏和知识库内容区页面排布，不再保留自行设计的正文并发/单篇跳过方案。
- 当时的页面契约不存在顶部文档切换栏；桌面是文档目录、位于正文左侧的本文目录和正文，移动端是右下目录按钮与全屏目录。该历史桌面顺序已由 2026-08-28 的 ADR-033 替代；图片/白板跳块和附件整篇原文回退继续有效。
- 阶段性成功快照在对齐期间继续供学生读取，直至被完整成功快照原子替代；后续同步失败不得替换当前快照。管理员 `/admin/knowledge` 页面与 `POST /api/v1/admin/knowledge/sync` 保持不变，最终更新继续由 Worker 执行。
- 正式计划仍为 `.agents/plans/plan_feishu_knowledge_sync.md`，已追加唯一正式修订；本次不涉及数据库迁移。

## 最终状态

已完成参考提交 `c28f8a0` 的同步办法与知识库内容区排布对齐、完整质量门、定向镜像部署和真实完整同步。

- 2026-08-27 前台运行以 `succeeded` 完成 228 个目录节点、212 篇文档和 977 个成功媒体引用，耗时 `00:33:28.757715`；13 次允许的资源回退未阻断快照，旧 48 篇阶段性快照已被自然替代。
- 最新 `sync_knowledge` Outbox 为 `sent`、`attempt_count=1` 且无错误；管理员手动更新接口保留，后续更新继续由 Worker 执行。
- 29 项后端知识库定向测试、完整后端 213 项测试、前端 20 个文件/76 项测试、Ruff、格式检查、严格 Mypy、ESLint、严格 TypeScript 和 Next.js 生产构建全部通过。
- Backend/Worker 镜像为 `sha256:f8c42b…`，Frontend 为 `sha256:e82d020…`；六服务 healthy，四个健康端点为 200，知识库页面匿名访问为 307，两个知识库 API 匿名访问为 401。
- Alembic 保持 `20260827_0011`，本轮无迁移；定向热修未纳入并行账号清理代码，账号清理工作树保持原样。

## 2026-08-28：知识库白色主题与右侧本文目录

- 用户要求覆盖此前参考页面排布：知识库内容区改为白色主题，文档目录保留左侧，本文目录移到正文右侧，成功打开文档后左侧文档目录自动收起。
- 唯一正式计划继续使用 `.agents/plans/plan_feishu_knowledge_sync.md`，不新增重复计划。
- 已完成：阅读器和块渲染器切换为白色主题，桌面 DOM/视觉顺序为文档目录—正文—本文目录；成功打开当前或其他文档后左目录自动收起，加载失败保持当前文档和目录状态。
- 验证通过：知识库定向 7 项测试、完整前端 20 文件/76 项测试、ESLint、严格 TypeScript、工作树和隔离源码的 Next.js 生产构建。
- 隔离部署完成：构建差异只有知识库阅读器、块渲染器和测试 3 个文件；Frontend 镜像为 `sha256:1842c4d…`，未纳入并行账号改动。六服务 healthy，四个健康端点为 200，知识库页面/API 匿名守卫为 307/401。
- 最新同步保持 `succeeded`、212 篇文档、977 个媒体引用，Outbox 保持 `sent`、`attempt_count=1`；未触发新同步。
- 本次不修改同步器、管理员手动接口、Worker、快照数据或数据库迁移。


## 2026-08-28：知识库主导航与本文目录交互修正

- 用户澄清：成功打开文档后应折叠系统最左侧主要导航，不自动改变文档目录；加载失败时两者保持原状态。
- 已实现共享主导航受控折叠事件、右侧本文目录 sticky/最大高度/内部滚动与当前章节高亮，并统一文档目录及按键的系统样式。
- 已通过知识库定向 8 项、完整前端 20 文件/77 项测试、ESLint、严格 TypeScript 和 Next.js 生产构建。
- 唯一正式计划仍为 `.agents/plans/plan_feishu_knowledge_sync.md`；隔离镜像构建、Frontend 替换和运行态验收已完成。
- 隔离差异仅为共享主导航、折叠事件、知识库阅读器和知识库测试；Frontend 镜像为 `sha256:2ad76bd…`，六服务 healthy，四个健康端点 200，页面/API 匿名守卫 307/401。
- 最新同步仍为 `succeeded`、212 篇文档、977 个媒体引用，Outbox 仍为 `sent`、`attempt_count=1`；未触发新同步。
- 本轮不修改同步器、管理员手动接口、Worker、Backend、快照或数据库迁移。


## 2026-08-28：意向调查创建 500 修复

- 用户反馈管理员“新建意向调查”显示“服务暂时无法处理请求”。Backend 日志确认父调查尚未落库时先插入选项，触发 `intention_options.survey_id` 外键失败；原失败事务已回滚，无半成品数据。
- 已在 `IntentionService.create()` 加入父调查后显式 `flush()`，再写入选项并提交；新增严格持久化顺序回归，不改变 API、Schema、数据库结构或迁移。
- 已通过意向调查定向 10 项、隔离源码完整后端测试、Ruff、146 个 Python 文件格式检查、146 个源文件严格 Mypy 和真实 PostgreSQL 回滚冒烟；测试调查与审计记录均为 0。
- 隔离构建只包含本次服务和测试修复，未纳入并行账号活跃度/清理改动。Backend/Worker 已统一部署镜像 `sha256:409a55ff…`；六服务 healthy，四个健康端点为 200，Alembic 为 `20260827_0011 (head)`，最近日志无新异常。

## 当前状态

已完成：根因定位、最小修复、自动化质量门、真实数据库回滚冒烟、隔离镜像部署与运行态验收全部通过。管理员可以重新创建单选或多选意向调查。

## 2026-08-28：意向调查升级为多题实名问卷

- 用户界面已统一改为“问卷”，支持动态配置 1～30 道必答单选/多选题，以及每人 1～100 次或不限的提交上限；学生看到已提交/剩余次数，达到上限后表单只读。
- 管理员可以查看按题统计和实名提交名单，名单包含姓名、学号、学校邮箱、最新答案、补充说明、累计提交次数和最后提交时间；学生及管理员学生视图均不能访问名单。
- 新增 `20260828_0013` 并接在工作树已有 `20260827_0012` 后，旧单题调查/选项/回答兼容迁移；唯一正式计划仍为 `.agents/plans/plan_student_teams_auto_assignment_intentions.md`。

## 最终状态

该阶段初轮已完成源码、权威文档与自动化验证：后端 218 项、前端 20 文件/79 项、ESLint、严格 TypeScript、生产构建、问卷定向 Ruff/格式/严格 Mypy和双向离线 PostgreSQL DDL 均通过。当时尚未部署，Docker socket 权限和既有账号清理代码的未定义 `payload` 分别阻塞真实往返与完整静态门；后续分提交和部署阶段已处理。

## 2026-08-28：管理员角色按钮无请求修复

- 用户反馈“设为管理员”点击后没有反应；运行日志显示用户管理页读取正常，但现场点击期间没有任何 `/admin/users/{id}/role` 请求。
- 根因是前端用 `new FormData(form).get("intent")` 读取提交按钮值；浏览器从表单构造 `FormData` 时不会自动包含本次提交按钮，导致 `intent` 为空且角色、禁用和恢复操作静默跳过。
- 改为从原生 `SubmitEvent.submitter` 读取实际点击按钮的值，并新增“设为管理员”回归，断言请求方法、路径、角色、原因及响应后的角色状态。
- 工作树账号管理定向 7 项、完整前端 20 文件/79 项、ESLint、严格 TypeScript 和生产构建通过；隔离热修源码完整前端 19 文件/71 项、ESLint、严格 TypeScript 和容器生产构建通过。
- 从当前运行基线的干净提交单独构建，只叠加角色按钮修复，未纳入并行账号清理和问卷改造；Frontend 已替换为 `sha256:f9cc2771…`，Nginx 保持主机 5000 端口。
- 六个常驻服务 healthy，`live`、`ready`、`worker`、`nginx-health` 均为 200，`/admin/users` 匿名访问为 307；Alembic 保持 `20260827_0011 (head)`。
- 上线后真实管理员页面已产生两次角色变更、一次禁用和一次恢复请求，四次写请求均返回 200，确认受影响按钮在运行态恢复。
- 本次不修改后端角色规则、API、数据库、迁移或现有用户角色数据。

## 当前状态

已完成：根因定位、回归测试、隔离镜像构建、Frontend/Nginx 替换和运行态健康验收。管理员刷新页面后可重新使用“设为管理员”。

## 2026-08-28：账号状态标签 UI 优化

- 用户反馈用户卡片直接显示 `active / disabled` 且硬色方块观感较差；现改为中文语义胶囊：“正常”“待验证”“已禁用”。
- 三种状态分别使用低饱和绿、琥珀、红浅色背景与边框，并加入状态圆点；管理员/学生角色改用蓝灰胶囊，超期提示收敛为带时钟图标的“X 天未登录”。
- 用户搜索现在同时支持“管理员 / 学生”和三种中文账号状态，不要求管理员输入内部枚举。
- 新增三状态语义、样式、英文枚举隐藏和中文搜索回归；工作树定向 8 项、完整 20 文件/80 项测试、ESLint、严格 TypeScript、Next.js 生产构建通过。
- 隔离运行基线通过完整 20 文件/72 项测试及最终定向测试、ESLint、严格 TypeScript和容器生产构建；仅包含上轮角色按钮修复与本轮状态 UI，未纳入并行账号清理和问卷改造。
- Frontend 当前镜像为 `sha256:4ebd888c…`；生产包确认包含“待验证”等新文案。六服务 healthy，四个健康端点 200，Nginx 保持 5000 端口，管理员页面匿名守卫为 307。
- 本次不修改 API、后端状态机、权限、数据库、迁移或用户数据。

## 当前状态

已完成：UI 调整、中文搜索、回归测试、隔离构建、Frontend 上线与运行态健康验收。刷新用户管理页即可看到新标签。

## 2026-08-28：分 commit 提交当前内容并尝试高并发压力测试

- 修复账号服务中未定义 `payload` 的错位代码，把邮箱变更前的 Session 锁从 `restore_user()` 移回 `patch_user()`。
- 已拆分三个实现 commit：`b328035`（账号活跃度与受保护清理后端）、`2468f05`（多题实名问卷后端与 `0013`）、`86a90c4`（账号管理及问卷前端）。
- 完整质量门通过：Ruff、161 文件格式检查、146 个源文件严格 Mypy、218 项后端测试；前端 ESLint、严格 TypeScript、20 文件/80 项测试与 Next.js 生产构建；Alembic 单一源码 head 为 `20260828_0013`。
- Docker 授权后已新建独立 `pnx-loginburst-20260828a`，空库迁移到 `20260828_0013` 并生成 300 个虚构学生容量数据；没有接入真实 SMTP、飞书或现有 5000 数据。
- 对现有 5000 入口完成无写入的 100 并发、10 轮混合探测，共 5,000 请求、0 错误，持续 4.077 秒、吞吐 1,226.374 req/s，总体 P95 286.163 ms、P99 330.053 ms、最大 376.940 ms。
- 隔离环境 100 个账号同出口同时登录只有 21 个成功，79 个被 Nginx `5r/s`、`burst=20` 粗限流以 503 拒绝；直连 Backend 对照 100/100 成功，总耗时 5.293 秒、P95 5.235 秒并产生 100 个独立 Session。
- 根因已定位为 Nginx 全认证路径共用同一 IP 突发桶；Backend、PostgreSQL 均保持健康且无半写入。入口尚未修改，需保留应用失败限流并单独修复校园 NAT 误伤及 503/429 契约不符。
- 隔离容器、卷、网络、专用镜像和临时密码目录已全部清理；未调用账号删除、问卷写入、邮件、飞书同步或上传接口。

## 当前状态

实现和权威文档已分 commit 整理，源码质量门、短时只读并发探测和 100 账号同时登录诊断已完成。隔离压测本身未部署或迁移 5000 实例；其后账号活跃度与问卷已独立部署，当前运行环境为 `20260828_0013`。

当前入口不支持同一出口 100 账号同时登录；Nginx 限流整改需另行实现并回归。正式 NFR-T03 的 100 Session 业务读取、20 路 multipart 和 `0011 ↔ 0012 ↔ 0013` 真实 PostgreSQL 往返仍待执行，不能用本次登录突发诊断替代。

## 2026-08-28：账号活跃度与多题实名问卷运行态部署

- 构建并部署最新提交的 Backend/Worker 与 Frontend 固定镜像；本地 Compose 标签已更新为 `questionnaire-account-20260828`。
- PostgreSQL 17 迁移前备份和生产副本隔离往返通过，运行库已从 `0011` 升级到 `0013` 且无模型漂移；没有执行账号删除。
- 旧调查、选项、回答和选择关系迁移完整，六服务 healthy，健康、匿名守卫、问卷/名单权限、编译包功能和知识库快照验收通过。
- 部署细节、备份校验和与回滚边界已同步到运维报告、项目基线、正式计划和变更记录。

## 当前状态

已完成账号活跃度与多题实名问卷的构建、迁移、服务替换和运行态验收。正式 NFR-T03 登录态容量与 20 路 multipart 仍是独立容量验收项，不阻塞本次功能部署。

## 2026-08-28：问卷查看、草稿编辑与运行态部署

- 管理员现在可查看任意状态问卷的完整说明、时间窗口、提交限制、题目和选项；仅草稿提供编辑，开放后继续只读以保护已有回答语义。
- PATCH 使用 revision 乐观锁并在单事务内整体替换草稿题目，成功返回完整刷新详情；学生和管理员学生视图不能访问管理详情或修改。
- 完整后端 230 项、前端 20 文件/82 项和全部静态质量门、生产构建通过；本轮无数据库迁移。
- 新固定镜像已部署，六服务 healthy，四健康端点 200，接口匿名守卫、编译包功能、问卷数据与知识库快照不变性均通过。

## 当前状态

已完成问卷内容查看、草稿编辑、备份、构建、部署和运行态验收。已开放、关闭和归档问卷仍只读；如果未来需要修改已有回答问卷，应设计版本化复制而不是原位改题。

## 2026-08-28：反馈答疑功能（已完成并部署）

- 学生可在 `/help` 提交“系统反馈”或“问题答疑”，筛选和查看本人记录；管理员在 `/admin/help` 查看全部工单并填写或修订答复。
- 新增 `help_requests` 分层后端、六个受保护 API、`20260828_0014` 可降级迁移、双角色导航、服务端安全 Markdown、revision 冲突保护及答复—审计—站内通知原子事务。
- 学生越权与不存在统一 404；普通管理员视图不能走学生接口，管理员学生视图只使用本人学生路径，管理接口只允许真实管理员。
- 唯一正式计划：`.agents/plans/plan_feedback_help_requests.md`。

## 当前状态

源码、契约和自动化质量门已完成：完整后端 242 项、Ruff、169 文件格式、153 个源文件严格 Mypy，完整前端 21 文件/87 项、ESLint、严格 TypeScript和 Next.js 生产构建均通过。

2026-08-29 已完成备份、生产副本隔离迁移往返、固定镜像构建、运行库 `0013 → 0014`、服务替换和 5000 入口验收。六服务 healthy，四健康端点 200，页面/API 匿名守卫为 307/401，旧问卷与知识库数据不变；生产降级会删除反馈答疑数据，产生工单后必须优先前滚修复。

## 2026-08-29：问题答疑解答后公开（已完成并部署）

- 仅有效登录用户可查看已解答的问题答疑；待解答问题和全部系统反馈继续私密。
- 公开响应不包含提问者 UUID、姓名、学号或邮箱，公开状态由现有类型与状态派生，不新增迁移。
- 保留本人私密工单和管理员处理路径，新增公开列表/详情 API、学生页面区域、公开详情及权限与脱敏回归。
- 唯一正式计划：`.agents/plans/plan_feedback_help_requests.md`。
- 完整质量门通过：后端 Ruff、169 文件格式、120 个源文件严格 Mypy、246 项 Pytest；前端 ESLint、严格 TypeScript、21 文件/89 项 Vitest 与 Next.js 生产构建。
- Backend/Worker `sha256:942b9ee5e98d…` 与 Frontend `sha256:93feb800a8c6…` 已部署固定标签 `help-public-20260829`；六服务 healthy，四健康端点 200。
- 运行 OpenAPI 为八个反馈答疑操作，公开页面匿名 307、公开 API 匿名 401；Alembic 保持 `20260828_0014` 且无模型漂移，本轮无迁移。
- 运维收尾已完成：用户授权后已撤销本次 Docker socket 临时 `user:pnx:rw-` ACL；`getfacl -cp /var/run/docker.sock` 复核不再包含命名用户 `pnx` 条目，当前无该部署遗留项。




## 2026-08-29：学生提醒分类与徽标消除（已完成并随本轮部署）

- 学生侧栏把有效未读按公告、作业、校内赛和反馈答疑分别显示，公告入口不再混入答疑解决、作业延期或私密评语提醒。
- 公告归档在同一事务把该公告未读提醒标为已读，查询侧排除历史归档公告；公告和本人工单详情通过单条已读 POST 自动刷新对应徽标。
- 工作台 API 保留 `unread_count` 并新增结构化 `unread_counts`；本人反馈答疑详情只返回当前用户当前工单的未读提醒 ID。
- 唯一正式计划：`.agents/plans/plan_student_notification_badges.md`；本轮无数据库迁移、无新依赖，已包含在 `help-public-20260829` 固定镜像中部署。
- 最终源码核对补齐个人资料、登录设备和公开答疑详情的学生分类徽标；前端完整 22 个文件/90 项测试、ESLint、严格 TypeScript 和生产构建通过。
- 三个共享页面补丁已部署为 Frontend `notification-badges-20260829`（`sha256:fd13387f14a5…`）；Backend/Worker 容器未替换，仅为原镜像增加同名固定别名并把 `.env` 切到统一标签。六服务 healthy，四健康端点 200，三页面匿名守卫 307，Alembic 保持 `20260828_0014 (head)`。
- 本次部署临时 Docker socket ACL 已由用户撤销并复核，当前仅剩 `root:docker` 的基础 `0660` 权限。
